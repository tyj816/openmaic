# OpenMAIC 二次开发方案 - 服创大赛

## 项目目标
基于 OpenMAIC 开发"多模态 AI 互动式教学智能体"，满足服创大赛要求。

## 一、功能对照表

| 赛题要求 | OpenMAIC 现状 | 开发任务 | 优先级 | 说明 |
|---------|--------------|---------|--------|------|
| 本地知识库 RAG | ❌ 无 | 新增向量数据库和检索系统 | P0 | 核心差异 |
| 语音输入 | ⚠️ 仅有 TTS | 新增 ASR 语音识别 | P0 | 核心差异 |
| 文字输入 | ✅ 已有 | 无需开发 | - | 可复用 |
| 多轮对话意图理解 | ⚠️ 有对话能力 | 调整为主动提问机制 | P0 | 核心差异 |
| PDF 解析 | ✅ 已有 | 无需开发 | - | 可复用 |
| Word 解析 | ⚠️ 部分支持 | 增强解析能力 | P1 | 需增强 |
| 视频解析 | ❌ 无 | 新增视频处理模块 | P0 | 核心差异 |
| 图片解析 | ⚠️ 基础支持 | 增强 OCR 和理解 | P1 | 需增强 |
| PPT 生成 | ✅ 已有 | 无需开发 | - | 可复用 |
| Word 教案生成 | ⚠️ 简单支持 | 生成详细教案 | P0 | 需增强 |
| 动画创意 | ❌ 无 | 新增动画生成模块 | P0 | 核心差异 |
| 互动小游戏 | ⚠️ 有交互式 HTML | 扩展游戏类型 | P1 | 可复用 |
| 迭代优化 | ⚠️ 重新生成 | 智能调整机制 | P1 | 需优化 |
| **AI 讲解（OpenMAIC 核心）** | ✅ 已有 | **删除，不需要** | - | **核心差异** |
| **课堂回放（OpenMAIC 核心）** | ✅ 已有 | **删除，不需要** | - | **核心差异** |

## 二、核心差异说明

### OpenMAIC 原始定位
- **产品类型**: 在线互动课堂系统
- **核心价值**: AI 教师讲解 + 学生实时互动
- **关键功能**: 
  - 生成 PPT 内容 ✅
  - 生成 AI 讲解动作（语音、白板、激光笔）❌ 不需要
  - 课堂回放和互动 ❌ 不需要
  - 多智能体讨论 ❌ 不需要

### 赛题要求定位
- **产品类型**: 课件生成工具
- **核心价值**: 帮助教师快速制作高质量课件
- **关键功能**:
  - 多轮对话理解教学意图 🆕 需要新增
  - 多模态资料解析（视频、图片）🆕 需要新增
  - 本地知识库 RAG 增强 🆕 需要新增
  - 生成 PPT + Word 教案 ✅ 可复用
  - 生成动画和游戏 🆕 需要新增
  - 导出可编辑文件 ✅ 可复用

### 需要删除的模块
```bash
lib/playback/          # 课堂回放引擎
lib/action/            # Action 执行引擎（白板、激光笔等）
app/classroom/[id]/    # 课堂页面
components/whiteboard/ # 白板组件
components/roundtable/ # 圆桌讨论
```

### 需要调整的模块
```bash
lib/orchestration/     # 多智能体编排 → 改为意图理解对话
lib/generation/        # 删除 Action 生成，保留内容生成
```

### 需要新增的模块
```bash
lib/intent/            # 意图理解和主动提问
lib/media/             # 视频处理
lib/rag/               # RAG 知识库
lib/animation/         # 动画生成
lib/game/              # 游戏生成
lib/export/            # 增强的导出功能
```

## 三、技术架构设计

### 2.1 意图理解模块（核心新增）

#### 技术选型
- **对话管理**: 基于 OpenMAIC 的 LangGraph，调整为意图理解流程
- **状态管理**: Zustand（已有）
- **主动提问**: 基于 LLM 的问题生成

#### 目录结构
```
OpenMAIC/
├── lib/
│   ├── intent/
│   │   ├── intent-analyzer.ts     # 意图分析
│   │   ├── question-generator.ts  # 问题生成
│   │   ├── intent-state.ts        # 意图状态管理
│   │   └── intent-types.ts        # 类型定义
│   └── orchestration/
│       └── intent-graph.ts        # 🔄 调整：意图理解流程图
└── app/
    └── api/
        └── intent/
            ├── analyze/route.ts   # 分析意图
            ├── clarify/route.ts   # 生成澄清问题
            └── confirm/route.ts   # 确认意图
```

#### 核心数据结构
```typescript
interface TeachingIntent {
  // 基本信息
  topic: string;              // 教学主题
  targetAudience: string;     // 目标学生（年级/专业）
  duration: number;           // 课时（分钟）
  
  // 教学设计
  objectives: string[];       // 教学目标
  keyPoints: string[];        // 重点
  difficulties: string[];     // 难点
  
  // 课件要求
  style: string;              // 课件风格
  interactionDesign: string[]; // 互动设计
  
  // 状态
  confirmed: boolean;         // 是否确认
  missingFields: string[];    // 缺失字段
}
```

#### 实现步骤
1. 分析用户初始输入，提取已知信息
2. 识别缺失的关键信息
3. 生成针对性问题
4. 多轮对话直到完整
5. 最终确认

### 2.2 本地知识库 RAG 系统（核心新增）

#### 技术选型
- **向量数据库**: Chroma（轻量级，易部署）
- **Embedding 模型**: 
  - 中文：bge-large-zh-v1.5
  - 英文：text-embedding-3-small (OpenAI)
- **文档处理**: LangChain Document Loaders

#### 目录结构
```
OpenMAIC/
├── lib/
│   ├── rag/
│   │   ├── vector-store.ts      # 向量数据库封装
│   │   ├── embeddings.ts        # Embedding 生成
│   │   ├── retriever.ts         # 检索器
│   │   ├── document-loader.ts   # 文档加载
│   │   └── rag-chain.ts         # RAG 链路
│   └── knowledge-base/
│       ├── index.ts             # 知识库管理
│       ├── upload.ts            # 文件上传
│       └── processor.ts         # 文档处理
├── app/
│   └── api/
│       ├── knowledge-base/      # 知识库 API
│       │   ├── upload/route.ts
│       │   ├── query/route.ts
│       │   └── list/route.ts
│       └── rag/
│           └── retrieve/route.ts
└── data/
    └── knowledge-base/          # 本地知识库存储
        ├── documents/           # 原始文档
        ├── vectors/             # 向量数据
        └── metadata/            # 元数据
```

#### 实现步骤
1. 安装依赖
```bash
pnpm add @langchain/community chromadb langchain
pnpm add @xenova/transformers  # 本地 embedding
```

2. 创建向量存储服务
3. 实现文档上传和处理
4. 集成到现有生成流程

### 2.2 语音输入模块

#### 技术选型
- **浏览器端**: Web Speech API (免费)
- **服务端**: 
  - OpenAI Whisper API
  - 阿里云 ASR
  - 讯飞语音识别

#### 目录结构
```
OpenMAIC/
├── lib/
│   └── audio/
│       ├── asr-providers.ts     # ASR 服务商配置
│       └── speech-recognition.ts # 语音识别封装
├── components/
│   └── generation/
│       └── voice-input.tsx      # 语音输入组件
└── app/
    └── api/
        └── transcribe/
            └── route.ts         # 语音转文字 API
```

#### 实现步骤
1. 前端添加录音组件
2. 集成 Web Speech API 或调用后端 ASR
3. 实时转文字显示
4. 与现有输入框整合

### 2.3 视频解析模块（核心新增）

#### 技术选型
- **视频处理**: ffmpeg
- **关键帧提取**: opencv-python
- **字幕提取**: whisper / 云服务
- **视觉理解**: GPT-4V / Gemini Vision

#### 目录结构
```
OpenMAIC/
├── lib/
│   └── media/
│       ├── video-processor.ts   # 视频处理
│       ├── frame-extractor.ts   # 关键帧提取
│       └── subtitle-parser.ts   # 字幕解析
└── app/
    └── api/
        └── parse-video/
            └── route.ts
```

#### 实现步骤
1. 安装 ffmpeg
2. 实现视频上传和存储
3. 提取关键帧和音频
4. 使用 Whisper 提取字幕
5. 使用 Vision 模型理解画面内容

### 2.4 Word 教案生成模块（需增强）

#### 设计思路
OpenMAIC 有基础的教案支持，但需要生成更详细、结构化的教案。

#### 目录结构
```
OpenMAIC/
├── lib/
│   └── export/
│       ├── teaching-plan-generator.ts  # 教案生成
│       ├── teaching-plan-templates.ts  # 教案模板
│       └── docx-exporter.ts            # DOCX 导出
└── app/
    └── api/
        └── export/
            └── teaching-plan/route.ts
```

#### 教案结构
```typescript
interface TeachingPlan {
  // 基本信息
  title: string;
  subject: string;
  grade: string;
  duration: number;
  
  // 教学设计
  objectives: {
    knowledge: string[];    // 知识目标
    ability: string[];      // 能力目标
    emotion: string[];      // 情感目标
  };
  
  // 教学过程
  process: TeachingStep[];
  
  // 其他
  keyPoints: string[];      // 重点
  difficulties: string[];   // 难点
  methods: string[];        // 教学方法
  resources: string[];      // 教学资源
  homework: string[];       // 课后作业
  boardDesign: string;      // 板书设计
  reflection: string;       // 教学反思（预留）
}

interface TeachingStep {
  phase: string;            // 阶段名称
  duration: number;         // 时长
  activities: Activity[];   // 活动
}

interface Activity {
  teacher: string;          // 教师活动
  student: string;          // 学生活动
  purpose: string;          // 设计意图
}
```

#### 实现步骤
1. 基于 PPT 内容生成教学过程
2. 从知识库检索教学方法
3. 生成配套练习和作业
4. 导出为 .docx 格式

### 2.5 动画和游戏生成模块（核心新增）

#### 技术选型
- **动画**: 
  - Lottie (JSON 动画)
  - CSS3 动画
  - Three.js (3D 动画)
- **游戏**: 
  - Phaser.js (游戏引擎)
  - 自定义 HTML5 Canvas

#### 目录结构
```
OpenMAIC/
├── lib/
│   ├── generation/
│   │   ├── animation-generator.ts # 动画生成
│   │   └── game-generator.ts      # 游戏生成
│   └── templates/
│       ├── animations/            # 动画模板
│       └── games/                 # 游戏模板
└── app/
    └── api/
        ├── generate-animation/
        │   └── route.ts
        └── generate-game/
            └── route.ts
```

#### 实现步骤
1. 准备动画和游戏模板库
2. 使用 LLM 生成动画脚本
3. 渲染为 Lottie JSON 或 HTML5
4. 集成到课件中

## 三、开发计划（4 周）

### Week 1: 基础设施
- [ ] 搭建本地知识库 RAG 系统
- [ ] 实现文档上传和向量化
- [ ] 集成语音输入（ASR）
- [ ] 测试基础功能

### Week 2: 多模态解析
- [ ] 实现视频解析（关键帧 + 字幕）
- [ ] 增强图片解析（OCR + Vision）
- [ ] 实现参考资料关联机制
- [ ] 测试多模态输入

### Week 3: 意图理解和内容生成
- [ ] 优化教学意图理解
- [ ] 实现主动提问机制
- [ ] 开发动画生成模块
- [ ] 开发互动游戏模板
- [ ] 集成到生成流程

### Week 4: 测试和优化
- [ ] 端到端测试
- [ ] 性能优化
- [ ] 准备演示案例
- [ ] 编写文档和 PPT

## 四、演示场景设计

### 场景 1: 小学数学 - 分数加法
**输入**:
- 语音: "我想教小学四年级学生分数加法"
- 上传: 教材 PDF（第三章分数运算）
- 上传: 教学视频（分数加法动画演示）

**智能体提问**:
- "这节课的时长是多少？"
- "学生是否已经掌握了分数的基本概念？"
- "您希望包含哪些互动环节？"

**生成内容**:
- PPT（15 页，包含动画演示）
- Word 教案（教学目标、过程、练习）
- 互动小游戏（分数拼图）
- 知识点动画（分数相加的可视化）

### 场景 2: 高中物理 - 牛顿第二定律
**输入**:
- 文字: "高中物理，牛顿第二定律，包含实验演示"
- 上传: 实验视频（小车加速实验）
- 知识库: 物理教材、历年高考题

**智能体提问**:
- "是否需要推导公式？"
- "实验部分需要详细到什么程度？"
- "是否需要习题练习？"

**生成内容**:
- PPT（实验步骤、公式推导、应用案例）
- Word 教案（实验指导、安全注意事项）
- 交互式模拟（力与加速度关系）
- 课后习题（从知识库检索）

## 五、技术难点和解决方案

### 5.1 向量检索性能
**问题**: 大规模知识库检索慢
**解决**: 
- 使用 Chroma 的持久化存储
- 实现分块索引
- 添加缓存机制

### 5.2 视频处理耗时
**问题**: 视频解析时间长
**解决**:
- 异步处理 + 进度反馈
- 只提取关键帧（每 5 秒一帧）
- 使用云服务加速

### 5.3 多模态融合
**问题**: 不同格式资料如何关联
**解决**:
- 统一元数据格式
- 建立引用关系图
- 使用 LLM 理解关联性

### 5.4 动画生成质量
**问题**: AI 生成的动画可能不符合预期
**解决**:
- 准备高质量模板库
- 使用 few-shot 提示
- 支持人工调整

## 六、部署方案

### 本地部署（推荐用于比赛）
```bash
# 1. 克隆项目
git clone https://github.com/THU-MAIC/OpenMAIC.git
cd OpenMAIC

# 2. 安装依赖
pnpm install

# 3. 配置环境变量
cp .env.example .env.local
# 编辑 .env.local，填入 API Key

# 4. 初始化知识库
pnpm run init-kb

# 5. 启动服务
pnpm dev
```

### Docker 部署
```bash
docker compose up --build
```

## 七、提交材料清单

### 必需材料
1. ✅ 项目概要介绍（README.md）
2. ✅ 项目简介 PPT（使用 OpenMAIC 生成）
3. ✅ 项目详细方案（本文档）
4. ✅ 项目演示视频（录制实际操作）
5. ✅ 本地知识库资料（示例数据集）

### 可选材料
- 技术架构图
- 用户使用手册
- 测试报告
- 创新点说明

## 八、创新点总结

1. **深度意图理解**: 基于多智能体的主动提问机制
2. **多模态融合**: 支持文本、语音、PDF、视频、图片
3. **知识库增强**: 本地 RAG 系统，专业领域知识注入
4. **动态内容生成**: AI 生成动画和互动游戏
5. **迭代优化闭环**: 预览-反馈-再生成
6. **开源可扩展**: 基于 OpenMAIC 开源项目

## 九、参考资源

- OpenMAIC 官方文档: https://github.com/THU-MAIC/OpenMAIC
- LangChain 文档: https://python.langchain.com/
- Chroma 文档: https://docs.trychroma.com/
- Whisper API: https://platform.openai.com/docs/guides/speech-to-text
