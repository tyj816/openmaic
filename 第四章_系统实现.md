# 第四章 系统实现

## 4.1 系统架构

本系统采用前后端分离的架构设计，前端基于 Next.js 16 框架构建，后端采用 Next.js API Routes 实现无服务器函数式接口。整体架构遵循"三源融合"的设计理念，将教师意图、参考资料和知识库内容有机整合，形成完整的教学设计生成链路。

前端层面采用 React 18 的服务端渲染能力，通过 Turbopack 实现快速的开发构建。页面路由采用文件系统路由，主要包含教师意图理解页（`/teaching-design/intent`）和教学设计工作台页（`/teaching-design/workspace`）两个核心路由。状态管理方面，系统使用 Zustand 管理全局状态，同时结合 React Hooks 管理组件级状态，避免了过度的状态提升。UI 组件库选用 shadcn/ui 配合 Tailwind CSS，确保了界面的一致性和响应式设计。动画交互通过 framer-motion 实现，为用户提供流畅的视觉反馈。

后端架构采用模块化设计，核心生成逻辑封装在 `lib/generation` 目录下。系统通过 API Routes 暴露 RESTful 接口，主要包括教学大纲生成（`/api/generate/teaching-outline`）、幻灯片画布生成（`/api/generate/teaching-slide`）、修改再生成（`/api/regenerate-teaching`）以及文档导出（`/api/generate-docx`）等端点。所有接口均采用同步返回模式，避免了复杂的异步任务管理，简化了前后端交互逻辑。

数据持久化方面，系统采用混合存储策略。用户上传的 PDF 文件和解析后的图片资源存储在浏览器的 IndexedDB 中，避免了重复上传和网络传输开销。生成的教学设计对象（TeachingDesign）在生成过程中保存在组件状态中，通过 sessionStorage 实现页面间的临时传递。这种轻量级的存储方案适合原型验证阶段，后续可扩展为基于数据库的持久化方案。

AI 流程编排是系统的核心，整个生成链路分为两个主要阶段。第一阶段是教学设计大纲生成，系统接收教师需求（TeachingRequest）、参考资料（ReferenceMaterial）和知识库查询结果，通过大语言模型生成包含教学目标、重难点、幻灯片结构和教学环节的完整设计框架。第二阶段是幻灯片画布生成，系统针对每一页幻灯片调用视觉生成模型，将抽象的教学要点转化为具体的 PPT 元素布局，包括文本框、图片、形状等可视化组件。这种两阶段设计既保证了教学内容的专业性，又实现了视觉呈现的美观性。

## 4.2 AI 流程编排

系统的 AI 流程编排采用管道式架构，通过多个独立的生成节点串联完成从教师意图到最终课件的转换。整个流程的核心在于 `teaching-outline-generator.ts` 模块，该模块实现了三源融合的上下文构建和多轮验证的生成策略。

流程的起点是教师意图理解阶段。系统通过对话式交互收集教师的教学目标、学科信息、年级层次和课时安排等基础信息。这些信息被结构化为 TeachingRequest 对象，作为后续生成的核心输入。与传统的表单填写不同，系统采用 AI 主动引导的对话模式，通过多轮追问逐步明确教师的真实需求，避免了信息遗漏和理解偏差。

参考资料解析是第二个关键节点。系统支持 PDF 文档和图片的上传，通过 `teaching-material-parser.ts` 模块完成资料的结构化提取。PDF 解析采用多提供商策略，支持 unpdf、Mathpix 和 Azure Document Intelligence 三种解析引擎，系统会根据文档特征自动选择最优方案。解析过程不仅提取文本内容，还会识别文档中的图片、表格和公式，将它们转换为结构化的 ParsedImage 对象。图片资源会被赋予唯一 ID 并存储在 IndexedDB 中，供后续的幻灯片生成使用。

知识库检索是三源融合的第三个来源。系统集成了 FastGPT 知识库服务，通过 `fastgpt-client.ts` 模块实现 RAG（检索增强生成）能力。当教师启用知识库增强选项时，系统会根据课题和教学目标构建专业的查询语句，从知识库中检索相关的教学知识、易错点分析和教学方法建议。检索结果不仅包含文本内容，还会保留每个知识片段的来源 ID（ragChunkId），为后续的来源追溯提供支持。

上下文融合是生成前的关键准备步骤。`teaching-context-builder.ts` 模块负责将三个来源的信息整合为统一的上下文包（TeachingContextBundle）。融合过程遵循结构化原则，将教师意图、参考资料和知识库内容分别标注来源，形成清晰的三段式上下文。这种设计既保证了信息的完整性，又为后续的来源标记提供了基础。

大纲生成是核心的 AI 调用环节。系统构建包含系统提示词和用户提示词的完整 Prompt，通过 `getModel` 函数获取配置的大语言模型实例，发起同步调用。系统提示词明确了输出格式要求，包括 JSON 结构定义、字段说明和来源标记规则。用户提示词则包含融合后的三源上下文和可用图片资源列表。模型返回的 JSON 响应经过 `parseJsonResponse` 函数修复和解析，转换为 TeachingDesign 对象。

生成结果验证是保证质量的重要环节。`fusion-validator.ts` 模块实现了软性验证机制，检查生成结果是否正确标记了内容来源，是否合理使用了参考资料和知识库内容。验证过程会统计三种来源的使用比例，检查 ragChunkId 的有效性，并生成详细的验证报告。如果发现关键问题，系统会自动触发重试机制，最多重试一次，并在重试时附加针对性的修正指令。

幻灯片画布生成是第二阶段的核心任务。系统遍历 TeachingDesign 中的每个 TeachingSlide，调用 `/api/generate/teaching-slide` 接口生成具体的视觉布局。生成过程会考虑幻灯片类型（封面页、内容页、过渡页、结束页）、教学要点、可用图片资源和视觉风格偏好。模型返回的 Slide 对象包含完整的 PPTElement 数组，每个元素定义了类型（文本、图片、形状）、位置、尺寸、样式等属性。生成的 canvas 对象会被赋值到 TeachingSlide.canvas 字段，供前端渲染使用。

修改再生成流程支持教师对生成结果的迭代优化。系统接收完整的 TeachingDesign 和修改指令，通过 `regenerate-teaching-design.ts` 模块调用大语言模型进行局部修改。模型会分析修改指令，识别需要调整的幻灯片，生成新的 TeachingDesign 对象。系统会智能检测哪些幻灯片发生了变化，只对修改的幻灯片重新生成 canvas，复用未修改的幻灯片，提高了再生成的效率。

整个 AI 流程的设计遵循可观测性原则，每个关键节点都会通过 `createLogger` 记录详细日志，包括输入参数、处理时长、输出结果和错误信息。前端通过回调函数接收生成进度，实时更新 UI 状态，为用户提供清晰的进度反馈。

## 4.3 知识库实现

知识库系统基于 FastGPT 平台构建，实现了向量化存储和语义检索能力。系统通过 `fastgpt-client.ts` 模块封装了与 FastGPT 的交互逻辑，提供了简洁的查询接口。

向量化处理是知识库的基础能力。教学领域的专业知识、教学方法、易错点分析等内容被预先切分为语义完整的知识片段，每个片段通过嵌入模型转换为高维向量表示。FastGPT 平台支持多种嵌入模型，系统默认使用 text-embedding-ada-002 模型，该模型在中文教学文本上表现出色。向量化后的知识片段存储在向量数据库中，每个片段都关联了元数据，包括来源文档、章节信息、知识类型等。

检索过程采用语义相似度匹配策略。当系统接收到教学设计请求时，会根据课题、学科和教学目标构建增强查询语句。查询语句不仅包含基本信息，还会明确知识需求，例如"请重点输出本课题的核心知识点、易错点及其解决方法、推荐的教学思路和教学方法"。这种结构化的查询方式提高了检索的精准度。

FastGPT 的检索流程分为两个阶段。第一阶段是向量检索，系统将查询语句转换为向量，在向量数据库中进行相似度搜索，召回 Top-K 个最相关的知识片段。第二阶段是重排序，系统使用交叉编码器对召回结果进行精排，确保返回的知识片段与查询意图高度匹配。检索结果会包含每个片段的相似度分数、来源信息和原始内容。

系统对检索结果进行了结构化处理。`queryFastGPT` 函数返回的 FastGPTQueryResult 对象不仅包含 AI 生成的综合答案，还包含原始的 quoteList 数组。每个 quote 对象记录了知识片段的 ID、来源文档名称、片段索引和具体内容。系统会将这些信息转换为 RagChunk 对象，保留完整的溯源链路。

来源追溯是知识库系统的重要特性。生成的教学设计中，每个标记为 `source: "knowledge"` 的知识点都会关联一个 ragChunkId，该 ID 对应知识库中的具体片段。验证模块会检查这些 ID 的有效性，确保标记的知识点确实来自知识库检索结果。这种设计不仅提高了内容的可信度，也为教师审核提供了便利。

知识库的容错机制保证了系统的稳定性。如果 FastGPT 服务不可用或查询超时，系统会优雅降级，继续使用教师意图和参考资料完成生成，而不会导致整个流程失败。日志系统会记录知识库查询的成功率和响应时长，为后续优化提供数据支持。

## 4.4 PPT 生成与 Word 生成实现

文档生成模块实现了从结构化数据到可交付文件的转换，支持 PPTX 和 DOCX 两种格式的导出。

PPT 生成采用纯前端方案，通过 `pptxgenjs` 库实现。核心函数 `buildPptxBlob` 位于 `lib/export/use-export-pptx.ts` 模块，该函数接收 TeachingDesign 对象，遍历其中的每个 TeachingSlide，将 canvas 字段中的 PPTElement 数组转换为 pptxgenjs 的幻灯片对象。转换过程会处理多种元素类型，包括文本框（text）、图片（image）、形状（shape）、线条（line）等。文本元素会保留字体、大小、颜色、对齐方式等样式属性；图片元素会从 IndexedDB 中读取 base64 数据并嵌入到 PPT 中；形状元素会转换为对应的几何图形。生成的 PPTX 文件包含完整的幻灯片内容，支持在 Microsoft PowerPoint 和 WPS 等软件中打开编辑。

Word 生成采用后端方案，通过 `docx` 库实现。核心函数 `generateDocxFromTeachingDesign` 位于 `lib/generation/docx-generator.ts` 模块，该函数将 TeachingDesign 对象转换为标准的教案文档。文档结构包含标题、基本信息表格、教学目标、教学重难点、教学过程、教学环节详细设计、课后作业、板书设计和教学反思等部分。每个 TeachingSlide 会被转换为一个教学环节，环节标题使用 Heading 2 样式，教学要点使用项目符号列表。系统完整保留了三源融合的来源标记，每个知识点后面会附加来源说明，例如"（教师设计）"、"（来自参考资料）"或"（来自知识库片段 xxx）"。生成的 DOCX 文件符合教案规范，可直接用于教学备课和存档。

两种文档格式的生成都采用直接映射策略，不涉及额外的 AI 调用，确保了生成速度和内容一致性。PPT 生成通常在 1-2 秒内完成，Word 生成在 100-500 毫秒内完成，满足了实时导出的需求。

## 4.5 多模态处理实现

多模态处理能力是系统的重要特性，支持文档解析、图像识别和视觉生成三个方面。

文档解析模块支持 PDF 格式的参考资料上传。系统通过 `/api/parse-pdf` 接口接收文件，根据文档特征选择合适的解析引擎。unpdf 引擎适用于纯文本 PDF，解析速度快但不支持复杂排版；Mathpix 引擎专门处理包含数学公式的文档，能够将公式转换为 LaTeX 格式；Azure Document Intelligence 引擎支持表格识别和复杂布局分析，适用于教材和讲义类文档。解析过程会提取文本内容、识别图片位置、记录页码信息，生成结构化的 ParsedMaterial 对象。

图像处理采用多层次策略。上传的图片和 PDF 中提取的图片会被赋予唯一 ID，存储在 IndexedDB 的 `teaching-images` 表中。每个图片对象包含原始数据（base64）、尺寸信息、页码位置和描述文本。系统支持视觉模型对图片进行理解，通过 `formatImagePlaceholder` 函数将图片转换为特殊的占位符格式，嵌入到 Prompt 中。视觉模型会分析图片内容，生成描述文本，辅助教学设计的生成。对于不支持视觉能力的模型，系统会使用 `formatImageDescription` 函数生成纯文本描述，确保图片信息不会丢失。

视觉生成是幻灯片画布生成的核心能力。系统支持配置图片生成提供商，包括 DALL-E、Stable Diffusion 和 Midjourney 等。当幻灯片需要配图时，系统会根据教学内容生成图片描述 Prompt，调用图片生成 API，将生成的图片嵌入到 PPTElement 中。生成的图片会被缓存在 IndexedDB 中，避免重复生成。

多模态处理的容错机制确保了系统的鲁棒性。如果 PDF 解析失败，系统会提示用户手动输入文本内容；如果图片生成失败，系统会使用占位符或跳过该图片；如果视觉模型不可用，系统会降级为纯文本模式。所有多模态操作都有详细的日志记录，便于问题排查和性能优化。

## 4.6 前端交互设计

前端交互设计遵循"对话驱动、预览为核心、闭环修改"的理念，为教师提供流畅的使用体验。

对话交互是系统的入口。意图理解页采用聊天式界面，AI 主动提出引导性问题，教师通过自然语言回答。对话历史以气泡形式展示，AI 消息使用渐变背景和机器人图标，教师消息使用纯色背景和教师图标。每条消息都附带元数据标签，例如"引导提问·第 1 轮"或"教师回答"，帮助用户理解对话进度。输入区域提供文本框、发送按钮、语音输入按钮和资料上传按钮，支持多种输入方式。

资料上传采用隐式入口设计。点击回形针按钮会展开上传卡片，卡片包含 PDF 上传和图片上传两个区域，以及已上传文件的列表。每个文件显示名称、类型、大小和解析状态，状态使用彩色徽章标识，例如"已解析"、"已入库"或"知识融合中"。上传过程使用 framer-motion 实现流畅的展开/收起动画，避免了突兀的界面跳转。

预览功能是工作台页的核心。中心区域展示大尺寸的幻灯片预览卡片，支持幻灯片预览和教案预览两种模式切换。幻灯片预览使用 ThumbnailSlide 组件渲染，该组件接收 Slide JSON 对象，通过 Canvas 2D API 绘制文本、图片和形状元素。教案预览以结构化文档形式展示，包含教学目标、教学过程、板书设计等部分。预览区域上方显示教学设计的元信息，包括标题、版本号、更新时间、教学目标、教学重点和教学难点。

结构导航提供快速定位能力。左侧栏展示 Slide 结构导航和教案结构两个卡片。Slide 导航以列表形式展示所有幻灯片，每个项目包含序号、类型、标题、描述和状态徽章。点击某个 Slide 会切换中心预览区的内容，当前选中的 Slide 使用渐变背景和阴影高亮显示。教案结构卡片展示教学目标、教学过程、板书设计和作业布置四个部分，每个部分显示简要说明。

修改再生成实现了闭环优化。预览区域下方提供修改面板，包含多行文本输入框和两个操作按钮。教师在输入框中描述修改需求，例如"弱化第 3 页文字密度，增加互动提问"，点击"再生成当前页"按钮后，系统会调用 `/api/regenerate-teaching` 接口，返回更新后的 TeachingDesign。前端会智能检测哪些 Slide 发生了变化，只刷新修改的部分，保持其他内容不变。修改过程中会显示加载状态，完成后自动切换到修改后的 Slide。

辅助信息区提供透明度支持。右侧栏展示引用与 RAG 证据卡片和工作流状态卡片。RAG 证据卡片列出所有来自知识库的知识点，每个项目显示来源类型（教师需求、上传资料、知识库）、片段 ID 和具体内容。工作流状态卡片展示教师意图理解、资料解析、知识融合、设计生成、预览修订五个步骤的完成情况，每个步骤使用图标和颜色标识状态。底部显示整体进度条，直观反映生成进度。

交互动画提升了用户体验。页面切换使用 framer-motion 的 AnimatePresence 实现淡入淡出效果，卡片悬停时会轻微上浮，按钮点击时会有缩放反馈。所有动画时长控制在 200-300 毫秒，既保证了流畅性，又不会让用户感到拖沓。

响应式设计确保了多设备兼容。系统采用 Tailwind CSS 的响应式工具类，在桌面端使用三栏布局，在平板端自动折叠侧栏，在手机端切换为单栏布局。所有交互元素都有足够的点击区域，文本大小和间距符合可访问性标准。

错误处理和用户反馈贯穿整个交互流程。API 调用失败时会显示友好的错误提示，生成超时时会提供重试选项，网络断开时会自动保存草稿。所有关键操作都有确认对话框，避免误操作导致数据丢失。系统使用 sonner 库实现 Toast 通知，成功、警告和错误消息使用不同的颜色和图标，确保用户能够快速识别。
