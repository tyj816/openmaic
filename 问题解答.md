# OpenMAIC 主产品层架构分析

## 一、产品入口类型

OpenMAIC 采用的是：**路由式产品页 + 单页工作区**

## 二、核心路由结构

### 1. 首页路由
- **路径**: `/` (根路由)
- **文件**: `OpenMAIC/app/page.tsx`
- **组件**: `HomePage` (默认导出)
- **功能**: 
  - 课程创建入口（PDF上传 + 需求输入）
  - 历史课堂列表（Recent Classrooms）
  - 用户配置（语言、主题、设置）

### 2. 生成预览路由
- **路径**: `/generation-preview`
- **文件**: `OpenMAIC/app/generation-preview/page.tsx`
- **功能**: 课程生成中间页（大纲生成、预览、编辑）

### 3. 课堂主工作区路由
- **路径**: `/classroom/[id]`
- **文件**: `OpenMAIC/app/classroom/[id]/page.tsx`
- **组件**: `ClassroomDetailPage`
- **核心容器**: `<Stage />` 组件

### 4. 教学对话测试路由
- **路径**: `/teaching-chat`
- **文件**: `OpenMAIC/app/teaching-chat/page.tsx`
- **功能**: 独立的教学对话测试页面

## 三、主布局层次

### 根布局 (RootLayout)
**文件**: `OpenMAIC/app/layout.tsx`

```
<html>
  <body>
    <ThemeProvider>
      <I18nProvider>
        <ServerProvidersInit />
        {children}  ← 路由页面插槽
        <Toaster />
      </I18nProvider>
    </ThemeProvider>
  </body>
</html>
```

**特点**:
- 全局 Provider 包裹
- 无固定头部/侧边栏
- 完全由子路由控制布局

## 四、课堂工作区架构 (核心)

### Stage 组件结构
**文件**: `OpenMAIC/components/stage.tsx`

```
<div ref={stageRef} className="h-screen flex flex-col">
  
  {/* 顶部导航栏 */}
  <Header currentSceneTitle={...} />
  
  <div className="flex-1 flex overflow-hidden">
    
    {/* 左侧场景导航栏 */}
    <SceneSidebar 
      collapsed={sidebarCollapsed}
      onToggleCollapse={...}
      onSceneSelect={gatedSceneSwitch}
    />
    
    {/* 中间内容区 */}
    <div className="flex-1 flex flex-col">
      
      {/* 画布区域（幻灯片/白板） */}
      <CanvasArea 
        scene={currentScene}
        playbackView={playbackView}
        whiteboardOpen={whiteboardOpen}
      />
      
      {/* 圆桌讨论区（悬浮气泡） */}
      <Roundtable 
        playbackView={playbackView}
        participants={participants}
        audioIndicatorState={audioIndicatorState}
      />
      
    </div>
    
    {/* 右侧聊天区域 */}
    <ChatArea 
      ref={chatAreaRef}
      collapsed={chatAreaCollapsed}
      width={chatAreaWidth}
      activeBubbleId={activeBubbleId}
    />
    
  </div>
</div>
```

### 布局特点
1. **三栏布局**: 左侧栏（场景列表） + 中间区（画布） + 右侧栏（聊天）
2. **可折叠**: 左右侧栏都支持折叠，状态持久化到 localStorage
3. **全屏模式**: 支持演示模式（Presentation Mode），自动折叠侧栏
4. **悬浮组件**: Roundtable 是绝对定位的悬浮气泡层

## 五、关键组件目录

### 顶部导航
- **文件**: `OpenMAIC/components/header.tsx`
- **功能**: 返回首页、场景标题、语言/主题切换、设置、导出

### 左侧栏
- **目录**: `OpenMAIC/components/stage/`
- **主文件**: `scene-sidebar.tsx`
- **功能**: 场景缩略图列表、场景切换、进度指示

### 中间画布
- **目录**: `OpenMAIC/components/canvas/`
- **主文件**: `canvas-area.tsx`
- **功能**: 幻灯片渲染、白板工具、画布工具栏

### 右侧聊天
- **目录**: `OpenMAIC/components/chat/`
- **主文件**: `chat-area.tsx`
- **功能**: 讲座记录、对话交互、笔记查看

### 圆桌讨论
- **目录**: `OpenMAIC/components/roundtable/`
- **主文件**: `index.tsx`
- **功能**: AI 代理对话气泡、语音指示器、讨论控制

## 六、状态管理架构

### Zustand Store 层
```
OpenMAIC/lib/store/
├── stage.ts          # 场景/课堂状态
├── canvas.ts         # 画布/白板状态
├── settings.ts       # 用户设置（布局、TTS、代理）
├── media-generation.ts  # 媒体生成任务
└── user-profile.ts   # 用户资料
```

### 关键状态
- `useStageStore`: 当前场景、场景列表、生成状态
- `useSettingsStore`: 侧栏折叠、聊天区宽度、选中代理
- `useCanvasStore`: 白板开关、画布工具状态

## 七、接管策略建议

### 方案 A: 壳层替换（推荐 ⭐）
**适用场景**: 需要完全重构主工作区布局

**实施方式**:
1. 创建新路由 `/classroom-v2/[id]`
2. 复用 Stage 组件的核心逻辑（PlaybackEngine、ActionEngine）
3. 重新编排布局组件（Header、Sidebar、Canvas、Chat）
4. 通过路由守卫或 feature flag 切换新旧版本

**优点**:
- 不破坏现有代码
- 可以渐进式迁移
- 方便 A/B 测试

### 方案 B: 工作区插槽替换
**适用场景**: 只需调整局部区域（如右侧栏）

**实施方式**:
1. 在 `Stage.tsx` 中添加插槽机制
2. 通过 props 或 context 注入自定义组件
3. 保持原有布局框架不变

**优点**:
- 改动最小
- 兼容性好
- 适合微调优化

### 方案 C: 路由守卫 + 新主编排页
**适用场景**: 需要完全不同的产品形态

**实施方式**:
1. 修改 `app/classroom/[id]/page.tsx`
2. 根据条件渲染不同的主容器组件
3. 新容器完全独立实现

**优点**:
- 最大灵活性
- 可以彻底重构
- 适合大版本升级

## 八、核心依赖关系

```
ClassroomDetailPage (路由页面)
  └─> Stage (主容器)
       ├─> Header (顶栏)
       ├─> SceneSidebar (左侧栏)
       ├─> CanvasArea (中间画布)
       │    ├─> SlideRenderer (幻灯片)
       │    └─> WhiteboardCanvas (白板)
       ├─> Roundtable (悬浮气泡)
       └─> ChatArea (右侧聊天)
            ├─> ChatSession (对话会话)
            └─> LectureNotesView (笔记视图)
```

## 九、关键技术点

1. **状态同步**: Zustand + localStorage 持久化
2. **实时通信**: SSE (Server-Sent Events) 用于 AI 流式响应
3. **媒体管理**: IndexedDB 存储生成的图片/视频
4. **播放引擎**: PlaybackEngine 控制讲座播放、讨论触发
5. **动作引擎**: ActionEngine 执行幻灯片动画、聚光灯等效果
6. **TTS 集成**: Azure TTS + 自定义音频播放器

## 十、总结

OpenMAIC 的主产品层是：
- **入口**: 单页首页 (`/`) + 路由式课堂页 (`/classroom/[id]`)
- **主布局**: 三栏工作区（左侧场景导航 + 中间画布 + 右侧聊天）
- **核心容器**: `Stage` 组件（1182 行，包含完整的播放引擎逻辑）
- **状态管理**: Zustand 多 store 架构
- **扩展方式**: 建议采用"壳层替换"方案，创建新路由并复用核心逻辑

如果你需要接管主工作区，最佳切入点是：
1. 复制 `Stage.tsx` 为 `StageV2.tsx`
2. 创建新路由 `/classroom-v2/[id]`
3. 保留 PlaybackEngine/ActionEngine 逻辑
4. 重新编排 UI 组件布局
5. 通过 URL 参数或 feature flag 控制版本切换

---

## 问题 2: 生成链路前后端接口形态

### 2.1 新建教学设计任务 API

**不存在独立的"新建任务" API**

当前流程：
1. 首页直接提交需求 → 跳转到 `/generation-preview`
2. 通过 `sessionStorage` 传递参数（无 HTTP 请求）
3. 在预览页自动触发生成流程

```typescript
// 首页提交逻辑 (app/page.tsx)
const sessionState = {
  sessionId: nanoid(),
  requirements,
  pdfStorageKey,
  pdfFileName,
  currentStep: 'generating'
};
sessionStorage.setItem('generationSession', JSON.stringify(sessionState));
router.push('/generation-preview');
```

### 2.2 上传资料 API

#### PDF 解析 API
- **URL**: `POST /api/parse-pdf`
- **请求体**: `multipart/form-data`
  ```typescript
  {
    pdf: File,
    providerId?: 'unpdf' | 'mathpix' | 'azure-di',
    apiKey?: string,
    baseUrl?: string
  }
  ```
- **返回体**:
  ```typescript
  {
    success: boolean,
    data?: {
      text: string,
      images: ParsedImage[],
      metadata: {
        pageCount: number,
        fileName: string,
        fileSize: number
      }
    },
    error?: string
  }
  ```
- **是否流式**: 否
- **是否异步**: 否（同步返回）
- **ID 字段**: 无（前端自己生成 storageKey 存 IndexedDB）

### 2.3 获取生成结果 API

#### 教学大纲生成 API
- **URL**: `POST /api/generate/teaching-outline`
- **请求体**:
  ```typescript
  {
    request: TeachingRequest,
    materials?: ReferenceMaterial[],
    imageMapping?: ImageMapping,
    visionEnabled?: boolean,
    researchContext?: string,
    modelString?: string,
    apiKey?: string,
    baseUrl?: string
  }
  ```
- **返回体**:
  ```typescript
  {
    design: TeachingDesign  // 完整的教学设计对象
  }
  ```
- **是否流式**: 否
- **是否异步**: 否（同步返回完整结果）
- **ID 字段**: `design.id` (前端生成的 nanoid)

#### 幻灯片画布生成 API
- **URL**: `POST /api/generate/teaching-slide`
- **请求体**:
  ```typescript
  {
    slide: TeachingSlide,
    assignedImages?: ParsedImage[],
    imageMapping?: ImageMapping,
    visionEnabled?: boolean,
    language?: 'zh-CN' | 'en-US',
    modelString?: string,
    apiKey?: string,
    baseUrl?: string
  }
  ```
- **返回体**:
  ```typescript
  {
    canvas: Slide  // 单个幻灯片的渲染数据
  }
  ```
- **是否流式**: 否
- **是否异步**: 否
- **ID 字段**: 无（直接返回 canvas 对象）

### 2.4 修改再生成 API

- **URL**: `POST /api/regenerate-teaching`
- **请求体**:
  ```typescript
  {
    design: TeachingDesign,  // 完整的当前设计
    instruction: string,      // 修改指令
    modelString?: string,
    apiKey?: string,
    baseUrl?: string,
    language?: string,
    imageMapping?: Record<string, string>,
    assignedImages?: any[]
  }
  ```
- **返回体**:
  ```typescript
  {
    success: boolean,
    design: TeachingDesign  // 返回完整的新设计（不是 patch）
  }
  ```
- **是否流式**: 否
- **是否异步**: 否
- **ID 字段**: `design.id` (保持不变), `design.version` (递增)

**关键特征**: 后端返回整份新的 TeachingDesign，不是局部 patch

### 2.5 PPTX 导出 API

**无独立 API，前端直接生成**

- **实现方式**: 前端使用 `pptxgenjs` 库直接生成
- **核心函数**: `buildPptxBlob()` (lib/export/use-export-pptx.ts)
- **流程**:
  ```typescript
  TeachingDesign → Scene[] → Slide[] → PptxGenJS → Blob → saveAs()
  ```
- **是否需要后端**: 否

### 2.6 DOCX 导出 API

- **URL**: `POST /api/generate-docx`
- **请求体**:
  ```typescript
  {
    teachingDesign: TeachingDesign
  }
  ```
- **返回体**: 直接返回文件流（application/vnd.openxmlformats-officedocument.wordprocessingml.document）
- **是否流式**: 否（文件下载）
- **是否异步**: 否
- **ID 字段**: 无

### 2.7 当前架构特征总结

**前端状态模型**: **document-centric（文档中心）**

- 核心实体: `TeachingDesign` (完整文档对象)
- 无独立的 `taskId` 或 `designId` 管理
- 无异步任务队列
- 无会话持久化（sessionStorage 临时存储）
- 生成流程在前端编排（generation-preview/page.tsx）

**问题**:
1. 无任务状态追踪（刷新页面丢失进度）
2. 无设计版本管理（只有内存中的 version 字段）
3. 无后端持久化（除了 IndexedDB 本地存储）
4. 修改再生成返回完整文档（无增量更新）

---

## 问题 3: 修改再生成的返回形态

**后端返回**: 整份新的 TeachingDesign

**证据**:
```typescript
// app/api/regenerate-teaching/route.ts
return NextResponse.json({
  success: true,
  design: updatedDesign,  // 完整的新设计对象
});
```

**前端处理**: 直接替换整个文档
```typescript
// 无合并逻辑，直接覆盖
setTeachingDesign(updatedDesign);
```

**当前问题**:
- 无局部更新能力
- 无 diff/patch 机制
- 无版本历史记录
- 无回滚功能

**建议**: 前端维护完整 TeachingDesignViewModel + 版本快照栈

---

## 问题 5: 当前预览能力

**预览方式**: 前端自己渲染 JSON 结构

**核心组件**:
- `SlideRenderer` (components/slide-renderer/Editor/)
- 输入: `Slide` 对象（JSON 结构）
- 输出: Canvas 渲染的幻灯片

**渲染流程**:
```
TeachingSlide.canvas (Slide JSON)
  ↓
SlideRenderer
  ↓
Canvas 2D 渲染 (PPTElement → 图形)
```

**特点**:
- 完全前端渲染（无后端预览服务）
- 支持实时编辑（白板模式）
- 支持导出为 PPTX（前端生成）
- DOCX 只支持下载，不支持预览

**可复用组件**:
- ✅ SlideRenderer（幻灯片渲染器）
- ✅ CanvasArea（画布容器）
- ✅ 导出按钮体系（Header 组件）
- ❌ 无独立的 artifact 容器（与 Stage 耦合）

---

## 问题 6: 教学设计领域模型

**已有稳定类型定义**: ✅

**核心类型文件**: `lib/types/teaching.ts`

### 关键类型

#### 输入层
```typescript
TeachingRequest {
  subject: string
  topic: string
  gradeLevel: string
  duration: number
  objectives?: { knowledge, skills, attitude }
  stylePreferences?: { tone, visualStyle, interactivityLevel }
  useKnowledgeBase?: boolean
  language: 'zh-CN' | 'en-US'
}

ReferenceMaterial {
  id: string
  type: 'pdf' | 'image' | 'pptx' | 'docx'
  parsedText?: string
  parsedImages?: ParsedImage[]
  metadata: { uploadedAt, size, pageCount }
}
```

#### 核心设计层
```typescript
TeachingDesign {
  id: string
  title: string
  subject: string
  gradeLevel: string
  duration: number
  objectives: { knowledge[], skills[], attitude[] }
  keyPoints: string[]
  difficulties: string[]
  slides: TeachingSlide[]
  procedures: TeachingProcedure[]
  homework?: string[]
  boardDesign?: string
  createdAt: Date
  updatedAt: Date
  version: number
}

TeachingSlide {
  id: string
  order: number
  title: string
  type?: 'cover' | 'contents' | 'transition' | 'content' | 'end'
  keyPoints: KeyPointWithSource[]
  contentBlocks: ContentBlock[]
  narration?: string
  canvas?: Slide  // 渲染数据
  relatedProcedureId?: string
}

KeyPointWithSource {
  content: string
  source?: 'teacher' | 'material' | 'knowledge'
  sourceDetail?: string
  ragChunkId?: string  // RAG 溯源
}
```

#### 三源融合层
```typescript
TeachingContextBundle {
  teacherIntent: { subject, topic, objectives, ... }
  extractedFromMaterials: ExtractedMaterials
  retrievedKnowledge: RetrievedKnowledge
  mergedContext: string
}

RetrievedKnowledge {
  relevantChunks: string[]
  references: string[]
  ragChunks?: RagChunk[]  // 带 ID 的结构化块
}
```

**类型完整度**: 高（已覆盖核心业务）

**缺失部分**:
- 无 `DesignVersion` 类型（版本管理）
- 无 `DesignDiff` 类型（变更追踪）
- 无 `GenerationTask` 类型（任务状态）

---

## 问题 7: 多模型/多配置调试入口

**当前状态**: 主产品层已暴露高级参数

**暴露位置**: 设置面板（SettingsDialog）

**可配置项**:
- 模型选择（providerId + modelId）
- API Key / Base URL
- 图片生成开关 + 提供商
- 视频生成开关 + 提供商
- PDF 解析提供商
- TTS 提供商 + 音色
- 播放速度
- 代理选择（Agent Registry）

**问题**: 设置面板过于复杂，不符合比赛交付要求

**建议**:
1. 主产品层只保留:
   - 学段（gradeLevel）
   - 学科（subject）
   - 课时/时长（duration）
   - 输出偏好（是否生成 PPT/DOCX）
2. 高级参数移到:
   - `/teaching-test` (已有测试页)
   - 或新建 `/lab` 路由
3. 通过环境变量或配置文件预设默认模型

**当前测试页**: `/teaching-test/page.tsx` (已存在，可直接扩展)
