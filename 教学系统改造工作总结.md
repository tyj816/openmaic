# 教学系统改造工作总结

## 1. 总体说明

本轮改造的目标不是推翻现有教学系统，而是在保留以下既有能力的前提下，替换并增强底层 PPT 生成主链：

- 教师需求输入与多轮澄清
- 参考资料上传与解析
- FastGPT 本地知识库 RAG
- 三源融合（教师需求 + 参考资料 + 知识库）
- 教学设计工作台
- PPT / DOCX 导出
- 页级再生成能力

核心改造思路是：

> **保留教学系统前半段能力，将原先“TeachingDesign → 单页 canvas”模式，逐步切换为基于 OpenMAIC 原生 scene workflow 的精简版教学课件生成流程。**

---

## 2. 本轮新增与修改的核心文件

本轮实际新增 / 修改的核心文件如下：

### 新增文件

1. `OpenMAIC/lib/generation/teaching-scene-workflow.ts`

### 修改文件

1. `OpenMAIC/app/api/generate/teaching-outline/route.ts`
2. `OpenMAIC/app/teaching-design/intent/page.tsx`
3. `OpenMAIC/app/api/regenerate-teaching/route.ts`
4. `OpenMAIC/lib/utils/regenerate-helpers.ts`
5. `OpenMAIC/components/teaching-design/workspace/WorkspaceSidebar.tsx`
6. `OpenMAIC/components/teaching-design/workspace/WorkspacePreviewPanel.tsx`

---

## 3. 核心功能改造内容

## 3.1 新增 scene-based 教学生成主链

### 涉及文件
- `OpenMAIC/lib/generation/teaching-scene-workflow.ts`

### 改造前
教学系统主链是：

```text
TeachingRequest
→ TeachingDesign（结构化摘要）
→ 对每一页调用 teaching-slide API 生成 canvas
→ 工作台预览 / 导出
```

该模式的问题是：

- 每页单独生成，缺少整课叙事上下文
- 页面信息密度不足
- PPT 整体成品感较弱
- 视觉质量不如 OpenMAIC 原生流程

### 改造后
新增了一个独立的 scene-based 教学生成器，主流程变为：

```text
教师需求 + 参考资料 + FastGPT 知识库
→ 三源融合上下文
→ scene outlines（课程结构）
→ scene content（页面内容）
→ scene assemble
→ 直接生成带 canvas 的 TeachingDesign
```

### 在新文件中完成的主要工作

#### （1）保留并接入三源融合能力
在 `teaching-scene-workflow.ts` 中继续保留：

- 教师需求结构化使用
- 资料解析结果使用
- FastGPT 检索结果使用
- `buildTeachingContextBundle(...)` 三源融合上下文构建

#### （2）新增 scene workflow 风格的输入构建
实现了：

- `buildKnowledgeQuery(...)`
- `buildRequirements(...)`

作用：
- 把教师需求、资料摘要、知识库内容组织为更适合 `generateSceneOutlinesFromRequirements(...)` 的输入文本
- 明确限制只生成 slide 类型
- 明确封面/目录/总结页由系统补充

#### （3）新增内容页 outlines 处理逻辑
实现了：

- `bodyOutlines(...)`
- `fullOutlines(...)`

作用：
- 先获取中间内容页 outlines
- 再自动补齐：
  - 封面页
  - 目录页
  - 课堂小测页
  - 总结页

#### （4）新增 keyPoint 来源映射逻辑
实现了：

- `sourceOf(...)`

作用：
- 根据内容命中情况将 keyPoint 标记为：
  - `teacher`
  - `material`
  - `knowledge`
- 如果命中知识库片段，还会保留：
  - `ragChunkId`
  - `sourceDetail`

这保证了工作台中的证据链展示仍然可用。

#### （5）新增 slide 类型与内容块构造逻辑
实现了：

- `slideType(...)`
- `blocks(...)`
- `procedures(...)`
- `difficulties(...)`

作用：
- 将 scene workflow 输出结果重新映射为现有 `TeachingDesign` 所需结构
- 与现有工作台、导出、证据链兼容

---

## 3.2 主生成 API 切换到新 scene workflow

### 涉及文件
- `OpenMAIC/app/api/generate/teaching-outline/route.ts`

### 改造内容
原来该接口调用的是旧链路中的：

- `generateTeachingDesignFromRequest(...)`

现在已改为调用：

- `generateTeachingDesignWithSceneWorkflow(...)`

### 带来的变化
这意味着前端无需修改 API 路径，就已经切换到底层新工作流。

也就是说：

- API 保持不变
- 生成结果结构保持 `TeachingDesign`
- 但底层 PPT 生成逻辑已经不再是旧的单页逐个补画模式

这是整个改造中最关键的一步。

---

## 3.3 前端生成流程去掉“逐页单独补 canvas”

### 涉及文件
- `OpenMAIC/app/teaching-design/intent/page.tsx`

### 改造前
前端逻辑是：

1. 调用 `/api/generate/teaching-outline`
2. 拿到 `TeachingDesign`
3. 再对每一页循环调用 `/api/generate/teaching-slide`
4. 把每页 canvas 补回 design

### 改造后
现在逻辑改为：

1. 调用 `/api/generate/teaching-outline`
2. 直接得到已经带 `canvas` 的完整 `TeachingDesign`
3. 保存到 session
4. 进入工作台

### 作用
这样做的收益是：

- 前端生成时间链路更短
- 减少逐页串行二次生成的复杂度
- PPT 页面直接受 scene workflow 驱动
- 页面上下文一致性更强
- 成品质量更接近 OpenMAIC 原生流程

---

## 3.4 再生成链路与新工作流保持一致

### 涉及文件
- `OpenMAIC/app/api/regenerate-teaching/route.ts`
- `OpenMAIC/lib/utils/regenerate-helpers.ts`

### 改造前存在的问题
原先再生成 API：

- 使用偏硬编码的模型配置
- 与主生成流程使用的模型解析方式不一致
- 再生成请求没有稳定带上当前图片与资料上下文

### 本轮改造内容

#### （1）统一模型解析方式
在 `/api/regenerate-teaching` 中，原先是偏手写的 GLM 配置。

现在改为统一使用：

- `resolveModel(...)`

这样再生成和主生成的模型解析方式保持一致。

#### （2）前端再生成请求会自动附带上下文
在 `regenerate-helpers.ts` 中，调用再生成接口时新增自动附带：

- 当前模型配置
- `imageMapping`
- `assignedImages`

来源于：

- `getCurrentModelConfig()`
- `loadTeachingDesignMaterials(...)`

#### （3）再生成后自动重建 canvas
在 `/api/regenerate-teaching` 中继续保留并稳定了如下能力：

1. 根据当前页指令重生成结构化 slide 内容
2. 找到被修改的 slide
3. 调用 `/api/generate/teaching-slide`
4. 为当前页重新生成 canvas

### 改造收益
这样做后：

- 再生成与主生成不会“模型脱节”
- 当前页修改后，右侧 PPT 真实预览会同步更新
- 图片资源在再生成场景中可继续使用
- 与现有工作台交互闭环保持一致

---

## 3.5 新增比赛版“课堂小测”轻量能力

### 涉及文件
- `OpenMAIC/lib/generation/teaching-scene-workflow.ts`
- `OpenMAIC/components/teaching-design/workspace/WorkspaceSidebar.tsx`
- `OpenMAIC/components/teaching-design/workspace/WorkspacePreviewPanel.tsx`

### 目标
赛题要求“内容生成多样性至少支持一种”。

在当前时间和项目节奏下，不适合直接引入复杂 interactive / PBL / HTML5 游戏，因此本轮采用轻量策略：

> **新增一个“课堂小测”页，作为比赛版的课堂检测页。**

### 实现内容

#### （1）自动补充一页课堂小测
在 `fullOutlines(...)` 中，新增：

- `课堂小测`

位置在：

- 内容页之后
- 总结页之前

#### （2）小测页内容不再只是普通标题页
新增了：

- `buildQuizKeyPoints(...)`

作用：
- 根据前面内容页提取核心点
- 生成更像测验结构的文案
- 包含：
  - 第几题
  - 围绕哪个知识点
  - 参考答案提示

#### （3）小测页单独使用专用 canvas 构造器
新增了：

- `buildQuizCanvas(...)`

作用：
- 为课堂小测页单独生成专门的 PPT 画布结构
- 不再完全依赖通用 slide content 生成结果
- 页面会包含：
  - 标题区
  - 3 个题卡
  - 题号 badge
  - 题干
  - 参考答案
  - “即时检测”标识
  - 更适合比赛演示的视觉层次

### 页面表现增强
在工作台中增加了对课堂小测页的识别与标注：

#### `WorkspaceSidebar.tsx`
新增：
- `比赛版课堂检测页` 标签

#### `WorkspacePreviewPanel.tsx`
新增：
- `课堂检测` badge

### 收益
这一步的意义是：

- 项目不再只有普通内容讲解页
- 已具备一种轻量“内容多样性”能力
- 能较好支撑赛题“至少支持一种”的要求
- 且实现成本远低于复杂互动页

---

## 4. 本轮改造后系统能力变化

## 4.1 主生成链路变化

### 改造前
```text
TeachingRequest
→ TeachingDesign 摘要
→ 对每页单独生成 canvas
→ 工作台 / 导出
```

### 改造后
```text
教师需求 + 资料 + FastGPT 知识库
→ 三源融合上下文
→ scene outlines
→ scene content
→ scene assemble
→ 带 canvas 的 TeachingDesign
→ 工作台 / 导出
```

### 直接收益
- PPT 页面的整体结构更稳定
- 成品感更强
- 页间上下文更完整
- 更接近 OpenMAIC 原生高质量工作流

---

## 4.2 再生成能力变化

### 改造前
- 再生成与主生成模型配置不统一
- 再生成上下文不完整
- 页面修改后可能无法稳定复原 canvas

### 改造后
- 再生成与主生成模型配置一致
- 自动带上图片资源与上下文
- 修改后自动重建 canvas
- 工作台中的“修改 → 预览更新”闭环更稳定

---

## 4.3 比赛版多样性能力变化

### 改造前
- 只有常规内容讲解页
- 无稳定展示型多样化页面

### 改造后
- 自动包含 `课堂小测` 检测页
- 小测页具备独立视觉结构
- 能作为比赛展示中的“多样性内容生成能力”样例

---

## 5. 当前已完成的改造点总结

本轮已经完成的改造可以归纳为以下 6 点：

### 1）新增 scene-based 教学生成主链
- 新增 `teaching-scene-workflow.ts`
- 从三源融合上下文直接走 scene workflow

### 2）主生成 API 切换到底层新工作流
- `/api/generate/teaching-outline` 已不再走旧主链

### 3）前端去掉逐页单独补 canvas 的旧模式
- 意图页直接拿到完整带 canvas 的 TeachingDesign

### 4）再生成链路与主链统一
- 模型配置统一
- 上下文传递增强
- 自动重建 canvas

### 5）比赛版新增课堂小测页
- 自动补充在课程结构中
- 作为轻量多样性能力实现

### 6）工作台对课堂检测页增加识别与展示
- 左侧导航增加标签
- 中间预览增加 badge

---

## 6. 本轮尚未处理 / 后续可继续优化的部分

虽然主链已经完成初步改造，但以下内容仍可继续优化：

### 1）普通内容页的课堂节奏感还可继续增强
例如：
- 更稳定区分概念页 / 例子页 / 方法页 / 总结页
- 强化页间叙事顺序

### 2）课堂小测页还可以进一步升级
例如：
- 增加更真实的题型结构
- 增加多选 / 判断 / 填空提示
- 更贴近 quiz 体验

### 3）局部再生成仍主要基于 teaching-slide 进行画布重建
后续可考虑进一步让再生成更深度对齐 scene workflow

### 4）工作台 UI 还可以增加更多结构化标识
例如：
- 导入 / 讲解 / 检测 / 总结分区标签
- 内容页的教学环节提示

---

## 7. 已完成改造的价值判断

综合来看，本轮改造已经完成了**从“教学设计摘要驱动 PPT”向“scene workflow 驱动 PPT”** 的关键转向。

它带来的核心价值包括：

### 1. 保留现有项目优势
没有推翻：
- RAG
- 三源融合
- 工作台
- 导出
- 再生成

### 2. 直接解决当前最大问题
即：
- PPT 质量弱于 OpenMAIC 原生工作流

### 3. 为比赛版构建了更合理的展示链路
现在项目已经能形成一条更清晰的比赛叙事：

```text
教师需求输入
→ 多轮澄清
→ 参考资料上传
→ FastGPT 本地知识库增强
→ 三源融合
→ 高质量教学课件生成
→ 工作台修改再生
→ PPT / DOCX 导出
```

### 4. 已具备“内容多样性至少一种”的实现基础
通过新增课堂小测页，已经为赛题要求提供了一个可展示的实现落点。

---

## 8. 结论

本轮改造完成后，系统已经实现了以下关键升级：

- 教学系统主生成链路已切换到基于 OpenMAIC 原生 scene workflow 的精简版
- 前端不再逐页单独补 canvas
- 再生成链路与主生成链路完成统一
- 新增比赛版课堂小测页作为内容多样性能力
- 工作台对新页面类型已具备展示支持

可以认为，本轮已经完成了“改造方案”中最关键的第一阶段落地：

> **在不推翻现有教学系统框架的前提下，成功完成了底层 PPT 生成主链的替换与增强。**

后续如果继续推进，建议优先围绕：

1. 普通内容页节奏与标题质量增强
2. 小测页进一步升级为更真实的测验体验
3. 再生成进一步深度对齐 scene workflow
4. 工作台教学结构标识增强

展开下一轮迭代。
