# OpenMAIC vs 赛题要求 - 工作流程对比分析

## 一、OpenMAIC 原始工作流程

### 核心定位
OpenMAIC 是一个**互动式在线课堂系统**，重点在于"教学过程的互动体验"。

### 完整流程

```
用户输入
  ↓
[阶段 1] 生成教学大纲
  - 调用 LLM 分析教学主题
  - 生成结构化大纲（SceneOutline[]）
  - 确定场景类型（slide/quiz/interactive/pbl）
  ↓
[阶段 2] 生成场景内容
  - 为每个场景生成具体内容
  - Slide: 生成 PPT 元素（文本、图片、形状等）
  - Quiz: 生成测验题目
  - Interactive: 生成 HTML5 交互页面
  - PBL: 生成项目制学习配置
  ↓
[阶段 3] 生成教学动作（Actions）⭐ 核心差异
  - 为每个场景生成 AI 教师的讲解动作
  - 语音讲解（TTS）
  - 白板绘图
  - 激光笔指向
  - 聚光灯效果
  - 多智能体对话
  ↓
[阶段 4] 课堂回放 ⭐ 核心差异
  - 用户进入"课堂"
  - AI 教师按照 Actions 序列讲解
  - 支持暂停、快进、互动提问
  - 多智能体讨论
  ↓
[可选] 导出
  - 导出 PPTX（静态）
  - 导出 HTML（交互页面）
```

### 技术架构重点

```typescript
// OpenMAIC 的核心数据结构
interface Scene {
  id: string;
  type: 'slide' | 'quiz' | 'interactive' | 'pbl';
  content: SlideContent | QuizContent | InteractiveContent | PBLContent;
  actions: Action[];  // ⭐ 这是 OpenMAIC 的核心
}

interface Action {
  type: 'speech' | 'whiteboard_draw' | 'spotlight' | 'laser' | ...;
  agentId: string;
  timestamp: number;
  // ... 各种动作参数
}
```

### 关键特点
- ✅ 生成完整的 PPT 内容
- ✅ 生成测验和交互页面
- ✅ **重点：生成 AI 教师的讲解动作序列**
- ✅ **重点：支持课堂回放和实时互动**
- ⚠️ 导出功能是次要的（因为重点在在线体验）

---

## 二、赛题要求的工作流程

### 核心定位
赛题要求的是一个**课件生成工具**，重点在于"帮助教师快速制作高质量课件"。

### 完整流程

```
用户输入（多模态）
  - 语音/文字描述教学意图
  - 上传参考资料（PDF、视频、图片）
  ↓
[阶段 1] 多轮对话理解意图 ⭐ 需要增强
  - AI 主动提问澄清需求
  - 确认教学目标、重点难点
  - 确认课件风格、互动设计
  - 多轮对话直到完全理解
  ↓
[阶段 2] 多模态资料解析 ⭐ 需要新增
  - PDF 解析（已有）
  - 视频解析（提取关键帧、字幕）
  - 图片解析（OCR、理解）
  - 建立资料与教学意图的关联
  ↓
[阶段 3] 知识库检索增强 ⭐ 需要新增
  - 从本地知识库检索相关内容
  - RAG 增强生成质量
  - 融合参考资料和知识库
  ↓
[阶段 4] 生成课件内容
  - PPT 演示文稿（已有）
  - Word 教案文档（已有）
  - 知识点动画（需要新增）
  - 互动小游戏（已有部分，需扩展）
  ↓
[阶段 5] 预览和迭代优化 ⭐ 需要调整
  - 教师预览生成的课件
  - 提出修改意见
  - AI 理解并调整
  - 重新生成（局部或全部）
  ↓
[阶段 6] 导出交付 ⭐ 核心目标
  - 导出 PPTX（可编辑）
  - 导出 DOCX（可编辑）
  - 导出动画（GIF/MP4/Lottie）
  - 导出游戏（HTML5）
```

### 技术架构重点

```typescript
// 赛题要求的核心数据结构
interface CoursewarePackage {
  ppt: {
    slides: Slide[];
    exportable: true;  // ⭐ 必须可导出为 .pptx
  };
  teachingPlan: {
    content: string;
    exportable: true;  // ⭐ 必须可导出为 .docx
  };
  animations: Animation[];  // ⭐ 知识点动画
  games: InteractiveGame[];  // ⭐ 互动小游戏
  // ❌ 不需要 Actions（不需要 AI 讲解）
  // ❌ 不需要课堂回放功能
}
```

### 关键特点
- ✅ 多轮对话理解教学意图
- ✅ 多模态资料解析和融合
- ✅ 本地知识库 RAG 增强
- ✅ 生成可编辑的课件文件
- ✅ 支持迭代优化
- ❌ **不需要 AI 教师讲解**
- ❌ **不需要课堂回放功能**

---

## 三、核心差异对比表

| 维度 | OpenMAIC | 赛题要求 | 差异分析 |
|------|----------|---------|---------|
| **产品定位** | 在线互动课堂 | 课件生成工具 | 完全不同 |
| **核心价值** | AI 教师讲解体验 | 减轻教师制作负担 | 目标不同 |
| **输入方式** | 文字 + PDF（可选） | 语音 + 文字 + 多种资料 | 需增强 |
| **意图理解** | 单次输入 | 多轮对话 + 主动提问 | 需增强 |
| **资料处理** | 仅 PDF | PDF + Word + 视频 + 图片 | 需新增 |
| **知识增强** | 无 | 本地知识库 RAG | 需新增 |
| **PPT 生成** | ✅ 已有 | ✅ 需要 | 可复用 |
| **Word 教案** | ⚠️ 简单支持 | ✅ 详细教案 | 需增强 |
| **动画生成** | ❌ 无 | ✅ 需要 | 需新增 |
| **互动游戏** | ⚠️ 有 Interactive | ✅ 需要 | 需扩展 |
| **AI 讲解** | ✅ 核心功能 | ❌ 不需要 | **可删除** |
| **课堂回放** | ✅ 核心功能 | ❌ 不需要 | **可删除** |
| **多智能体** | ✅ 讨论互动 | ❌ 不需要 | **可删除** |
| **TTS 语音** | ✅ 讲解用 | ⚠️ 仅输入用 | 调整用途 |
| **白板绘图** | ✅ 讲解用 | ❌ 不需要 | **可删除** |
| **导出功能** | ⚠️ 次要 | ✅ 核心 | 需增强 |
| **迭代优化** | ⚠️ 重新生成 | ✅ 智能调整 | 需增强 |

---

## 四、需要保留的 OpenMAIC 模块

### ✅ 完全保留（核心复用）

1. **PPT 生成引擎** (`lib/generation/scene-generator.ts`)
   - 生成 Slide 元素（文本、图片、形状、表格、图表）
   - 布局算法
   - 主题样式

2. **PPTX 导出** (`packages/pptxgenjs/`)
   - 导出为可编辑的 .pptx 文件
   - 这是赛题的核心要求

3. **PDF 解析** (`lib/pdf/`)
   - 已有的 PDF 解析能力
   - 支持 MinerU 增强解析

4. **Interactive HTML 生成** (`lib/generation/scene-generator.ts`)
   - 生成交互式 HTML 页面
   - 可作为"互动小游戏"的基础

5. **LLM 调用封装** (`lib/ai/`)
   - 多模型支持
   - 统一的 AI 调用接口

6. **大纲生成** (`lib/generation/outline-generator.ts`)
   - 分析教学主题
   - 生成结构化大纲

### ⚠️ 需要调整（部分复用）

1. **多智能体编排** (`lib/orchestration/`)
   - **保留**：多轮对话能力
   - **调整**：用于意图理解，而非课堂讨论
   - **删除**：课堂回放相关逻辑

2. **Action 生成** (`lib/generation/scene-generator.ts`)
   - **删除**：AI 讲解动作生成
   - **保留**：可能用于动画脚本生成

3. **TTS 语音** (`lib/audio/`)
   - **删除**：课堂讲解用的 TTS
   - **保留**：语音输入用的 ASR

### ❌ 可以删除（不需要）

1. **课堂回放引擎** (`lib/playback/`)
   - 完全不需要

2. **Action 执行引擎** (`lib/action/`)
   - 白板绘图、激光笔、聚光灯等
   - 完全不需要

3. **实时互动** (`app/classroom/[id]/`)
   - 课堂页面
   - 完全不需要

4. **多智能体讨论** (`lib/orchestration/director-graph.ts`)
   - 圆桌讨论、角色扮演
   - 不需要

---

## 五、需要新增的核心模块

### 1. 意图理解增强模块

```typescript
// lib/intent/intent-analyzer.ts
interface TeachingIntent {
  topic: string;
  targetAudience: string;  // 年级、专业
  duration: number;  // 课时
  objectives: string[];  // 教学目标
  keyPoints: string[];  // 重点
  difficulties: string[];  // 难点
  interactionDesign: string[];  // 互动设计
  style: string;  // 课件风格
  confirmed: boolean;  // 是否确认
}

// 主动提问机制
async function clarifyIntent(
  userInput: string,
  currentIntent: Partial<TeachingIntent>
): Promise<{ question: string; field: keyof TeachingIntent }> {
  // 分析缺失的信息
  // 生成针对性问题
  // 返回问题和对应字段
}
```

### 2. 多模态资料解析模块

```typescript
// lib/media/multi-modal-processor.ts
interface ProcessedMaterial {
  type: 'pdf' | 'video' | 'image' | 'word';
  content: {
    text?: string;
    images?: string[];
    keyFrames?: string[];
    subtitles?: string;
  };
  metadata: {
    source: string;
    relevance: string;  // 与教学意图的关联
  };
}

async function processAllMaterials(
  materials: UploadedFile[],
  intent: TeachingIntent
): Promise<ProcessedMaterial[]> {
  // 并行处理所有资料
  // 建立与教学意图的关联
}
```

### 3. RAG 知识库模块

```typescript
// lib/rag/knowledge-base.ts
interface KnowledgeBase {
  vectorStore: VectorStore;
  
  async query(
    topic: string,
    intent: TeachingIntent
  ): Promise<RetrievedKnowledge[]>;
  
  async addDocuments(docs: Document[]): Promise<void>;
}
```

### 4. Word 教案生成模块

```typescript
// lib/export/teaching-plan-generator.ts
interface TeachingPlan {
  objectives: string[];
  process: TeachingStep[];
  methods: string[];
  activities: Activity[];
  homework: string[];
  reflection: string;
}

async function generateTeachingPlan(
  intent: TeachingIntent,
  pptContent: Slide[],
  knowledgeBase: RetrievedKnowledge[]
): Promise<TeachingPlan> {
  // 基于 PPT 内容生成配套教案
}
```

### 5. 动画生成模块

```typescript
// lib/generation/animation-generator.ts
interface Animation {
  id: string;
  type: 'lottie' | 'css' | 'video';
  concept: string;  // 知识点
  script: AnimationScript;
  output: string;  // Lottie JSON / CSS / MP4
}

async function generateAnimation(
  concept: string,
  style: string
): Promise<Animation> {
  // 使用 LLM 生成动画脚本
  // 渲染为 Lottie JSON 或其他格式
}
```

### 6. 游戏生成模块

```typescript
// lib/generation/game-generator.ts
interface InteractiveGame {
  id: string;
  type: 'puzzle' | 'quiz' | 'simulation' | 'drag-drop';
  concept: string;
  html: string;  // 完整的 HTML5 游戏
}

async function generateGame(
  concept: string,
  gameType: string
): Promise<InteractiveGame> {
  // 基于模板生成游戏
  // 使用 LLM 填充内容
}
```

---

## 六、调整后的工作流程

### 新的生成流程

```typescript
// app/api/generate-courseware/route.ts

async function generateCourseware(input: CoursewareInput) {
  // ========== 阶段 1: 意图理解（多轮对话）==========
  let intent = await analyzeInitialIntent(input.userInput);
  
  while (!intent.confirmed) {
    const question = await generateClarificationQuestion(intent);
    // 等待用户回答（通过 WebSocket 或轮询）
    const answer = await waitForUserAnswer(question);
    intent = await updateIntent(intent, answer);
  }
  
  // ========== 阶段 2: 资料处理 ==========
  const processedMaterials = await processAllMaterials(
    input.uploadedFiles,
    intent
  );
  
  // ========== 阶段 3: 知识库检索 ==========
  const knowledgeBase = await queryKnowledgeBase(intent.topic);
  
  // ========== 阶段 4: 生成大纲 ==========
  const outline = await generateOutline({
    intent,
    materials: processedMaterials,
    knowledge: knowledgeBase
  });
  
  // ========== 阶段 5: 生成 PPT 内容 ==========
  // ✅ 复用 OpenMAIC 的 generateSceneContent
  const slides = await generateSlides(outline);
  
  // ========== 阶段 6: 生成 Word 教案 ==========
  // 🆕 新增模块
  const teachingPlan = await generateTeachingPlan(intent, slides);
  
  // ========== 阶段 7: 生成动画和游戏 ==========
  // 🆕 新增模块
  const animations = await generateAnimations(outline.keyPoints);
  const games = await generateGames(outline.interactionDesign);
  
  // ========== 阶段 8: 打包返回 ==========
  return {
    ppt: slides,
    teachingPlan,
    animations,
    games,
    exportUrls: {
      pptx: '/api/export/pptx',
      docx: '/api/export/docx',
      animations: '/api/export/animations',
      games: '/api/export/games'
    }
  };
}
```

---

## 七、开发优先级调整

### P0（必须完成，核心差异）

1. **多轮对话意图理解**
   - 实现主动提问机制
   - 意图确认流程
   - 对话状态管理

2. **视频处理模块**
   - 关键帧提取
   - 字幕提取
   - 与教学意图关联

3. **RAG 知识库系统**
   - 向量存储
   - 检索增强
   - 知识融合

4. **Word 教案生成**
   - 详细教案结构
   - 与 PPT 配套
   - 导出为 .docx

### P1（重要，增强效果）

1. **动画生成模块**
   - Lottie 动画
   - 知识点可视化

2. **游戏模板库**
   - 拼图、拖拽等
   - 基于模板生成

3. **语音输入**
   - ASR 集成
   - 实时转文字

### P2（可选，锦上添花）

1. **图片 OCR 增强**
2. **Word 文档解析**
3. **更多动画类型**

---

## 八、代码重构建议

### 删除的文件/目录

```bash
# 可以删除的模块（不影响课件生成）
lib/playback/          # 课堂回放引擎
lib/action/            # Action 执行引擎
app/classroom/[id]/    # 课堂页面
components/whiteboard/ # 白板组件
components/roundtable/ # 圆桌讨论
```

### 保留并调整的模块

```bash
# 保留核心生成能力
lib/generation/        # PPT 生成（保留）
lib/orchestration/     # 多轮对话（调整用途）
lib/ai/                # LLM 调用（保留）
lib/pdf/               # PDF 解析（保留）
packages/pptxgenjs/    # PPTX 导出（保留）
```

### 新增的模块

```bash
# 新增功能
lib/intent/            # 意图理解
lib/media/             # 多模态处理
lib/rag/               # RAG 系统
lib/export/            # 增强导出
lib/animation/         # 动画生成
lib/game/              # 游戏生成
```

---

## 九、总结

### OpenMAIC 的核心是什么？
**在线互动课堂体验** - AI 教师讲解 + 学生互动

### 赛题要求的核心是什么？
**课件生成工具** - 帮助教师快速制作高质量课件

### 最大的差异是什么？
- OpenMAIC: 生成内容 + **生成讲解动作** + **课堂回放**
- 赛题: **多轮理解意图** + 生成内容 + **导出文件**

### 如何复用 OpenMAIC？
1. ✅ **完全复用**: PPT 生成、PPTX 导出、PDF 解析
2. ⚠️ **调整复用**: 多智能体（用于对话）、Interactive（用于游戏）
3. ❌ **删除**: 课堂回放、Action 执行、白板绘图
4. 🆕 **新增**: RAG、视频处理、意图理解、动画生成

### 开发重点在哪里？
1. **意图理解**（多轮对话）
2. **多模态处理**（视频、图片）
3. **RAG 系统**（知识库）
4. **Word 教案**（详细生成）
5. **动画和游戏**（互动内容）

---

**结论**: OpenMAIC 提供了优秀的 PPT 生成基础，但需要大幅调整工作流程，删除课堂相关功能，新增意图理解和多模态处理能力。
