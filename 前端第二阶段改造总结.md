已按你要求完成第二步，只打通了“意图页真实生成 → workspace 真实展示”这条主链路，没有接 regenerate、多轮智能体、持久化或 Stage 体系。

## 1. 新增/修改了哪些文件

### 新增

- `OpenMAIC/lib/mappers/intent-to-teaching-request.ts`
- `OpenMAIC/lib/mappers/teaching-design-to-workspace.ts`
- `OpenMAIC/lib/utils/teaching-design-session.ts`

### 修改

- `OpenMAIC/app/teaching-design/intent/page.tsx`
- `OpenMAIC/app/teaching-design/workspace/page.tsx`
- `OpenMAIC/components/teaching-design/intent/IntentConversationPanel.tsx`
- `OpenMAIC/components/teaching-design/intent/UploadDrawerCard.tsx`
- `OpenMAIC/components/teaching-design/intent/IntentStatusPanel.tsx`
- `OpenMAIC/components/teaching-design/workspace/WorkspaceSidebar.tsx`
- `OpenMAIC/components/teaching-design/workspace/WorkspacePreviewPanel.tsx`
- `OpenMAIC/components/teaching-design/workspace/WorkspaceEvidencePanel.tsx`
- `OpenMAIC/components/teaching-design/StatusBadge.tsx`
- `OpenMAIC/lib/types/teaching-design-ui.ts`

------

## 2. 哪些 mock 已经被真实数据替换

### 已被真实数据替换

#### `/teaching-design/intent`

- 对话消息：改为页面内 state 驱动
- 上传文件列表：改为真实 PDF 解析后生成
- “生成教学设计”：改为真实请求 `/api/generate/teaching-outline`

#### `/teaching-design/workspace`

- 优先从 `sessionStorage` 读取真实 `TeachingDesign`
- 用 mapper 转成当前 UI 所需 ViewModel
- 当前激活 slide、预览内容、证据区、流程状态，均可由真实 design 驱动

### 仍保留 mock 作为 fallback

- `mockProjectSummary`
- `mockGuidancePrompts`
- `mockIntentMessages` 作为 intent 页初始引导内容
- workspace 在无 draft 时仍 fallback 到：
  - `mockDesignSummary`
  - `mockSlides`
  - `mockLessonSections`
  - `mockEvidenceItems`
  - `mockWorkflowSteps`

------

## 3. TeachingRequest 是如何从意图页组装出来的

我新增了：

- `lib/mappers/intent-to-teaching-request.ts`

核心策略是：

### 输入来源

- ```
  projectSummary
  ```

  - `subject`
  - `grade`
  - `topic`
  - `duration`

- 页面内的

   

  ```
  messages
  ```

  - 仅提取 `role === "teacher"` 的消息

- 已上传的 `materials`

### 映射规则

- `subject` ← `projectSummary.subject`

- `topic` ← `projectSummary.topic`

- `gradeLevel` ← `projectSummary.grade`

- `duration` ← 从 `projectSummary.duration` 提取数字

- `language` ← 当前固定 `zh-CN`

- ```
  objectives
  ```

  - 从教师消息中抽取简化版 `knowledge / skills / attitude`

- ```
  additionalNotes
  ```

  - 拼接教师补充信息 + 已上传资料名称

- ```
  uploadedMaterials
  ```

  - 使用当前 `materials` 的 `id`

- ```
  useKnowledgeBase
  ```

  - 当前默认 `true`

这一步没有接真正 LLM 总结，完全符合你要求的“前端 mapper 压缩意图内容”。

------

## 4. `generate/teaching-outline` 是如何接入的

在：

- `app/teaching-design/intent/page.tsx`

里点击“生成教学设计”时：

1. 用 `mapIntentToTeachingRequest(...)` 组装真实 `TeachingRequest`
2. 读取当前模型配置：
   - `getCurrentModelConfig()`
3. 调用：

POST /api/generate/teaching-outline

请求体包含：

- `request`
- `materials`
- `modelString`
- `apiKey`
- `baseUrl`
- `providerType`
- `requiresApiKey`
- `visionEnabled: false`

这样做是“最小必要参数”方案，没有把调试参数暴露到 UI 里。

成功后：

- 拿到真实 `TeachingDesign`
- 保存到 session
- 跳转 `/teaching-design/workspace`

------

## 5. workspace 是如何读取真实 design 的

我新增了：

- `lib/utils/teaching-design-session.ts`

包含：

- `saveTeachingDesignDraft`
- `loadTeachingDesignDraft`
- `clearTeachingDesignDraft`

### 工作流

#### intent 页

生成成功后：

saveTeachingDesignDraft(design)

router.push("/teaching-design/workspace")

#### workspace 页

页面加载时：

const draft = loadTeachingDesignDraft()

- 如果有 draft：
  - 转成 ViewModel
  - 用真实 design 驱动页面
- 如果没有：
  - 显示 warning
  - fallback 到 mock workspace 数据

所以本阶段没有引入 designId，也没有后端持久化，符合你的边界要求。

------

## 6. mapper 层是如何设计的

### A. `intent-to-teaching-request.ts`

职责很单一：

- 将意图页当前 UI 状态转成真实 `TeachingRequest`

重点：

- 不把组装逻辑散落在 page 里
- 后续第三步如果接入真实多轮 agent，只需要替换这个 mapper 的输入来源或增强提取逻辑

------

### B. `teaching-design-to-workspace.ts`

职责：

- 把 `TeachingDesign` 转成当前工作台需要的 `WorkspaceViewModel`

输出包括：

#### 1) 头部摘要 `designSummary`

- `title`
- `version`
- `updatedAt`
- `goals`
- `highlights`
- `difficulties`
- 可选 `boardDesign`
- 可选 `homework`

#### 2) Slide 导航 `slides`

每个 slide 映射为 UI `Slide`：

- `id`
- `type`
- `title`
- `tag`
- `status`
- `desc`
- `pageNo`
- `bullets`
- `narration`
- `hasCanvas`
- `canvasSummary`

#### 3) 教案结构 `lessonSections`

优先从 `procedures` 映射：

- `stageName`
- `duration`
- `teacherActivity`

没有 procedures 时做降级。

#### 4) 证据区 `evidenceItems`

从 `slides[].keyPoints[]` 读取：

- `source`
- `sourceDetail`
- `ragChunkId`

映射为：

- `教师需求`
- `上传资料`
- `知识库`

#### 5) 流程状态 `workflowSteps`

本阶段先固定 ViewModel 化，不接真实状态机，只体现主链路已打通。

------

## 7. 当前还保留了哪些 mock / fallback

### intent 页

保留：

- 初始课题信息 `mockProjectSummary`
- 初始 guidance chips `mockGuidancePrompts`
- 初始对话历史 `mockIntentMessages`

原因：

- 本阶段不接真实多轮 agent
- 但页面仍需可演示、可控

### workspace 页

保留完整 fallback：

- session 读不到真实 draft 时，回退到 mock workspace 数据
- 页面不会崩

### 预览策略

没有接 Stage / SlideRenderer 仍保持低风险预览：

- 标题
- bullets
- narration
- canvas 是否存在
- 画布摘要状态

这符合你要求的“半真实业务驱动”。

------

## 8. 是否存在类型不匹配问题

目前我检查过这次改动涉及的文件，没有 linter / 类型诊断错误。

本次处理过的类型点包括：

- `IntentMessage.id` 从 number 调整为 string

- `UploadedFile.id` 从 number 调整为 string

- workspace `activeSlideId` 改为 string，适配真实 `TeachingSlide.id`

- 扩展 UI

   

  ```
  Slide
  ```

   

  类型，增加：

  - `pageNo`
  - `bullets`
  - `narration`
  - `hasCanvas`
  - `canvasSummary`

- 新增 `WorkspaceViewModel`

所以第二步范围内当前是类型闭合的。

------

## 9. 哪些点最适合进入第三步（再生成闭环接入）

按你当前这套结构，第三步最自然的切入点有：

### 1. `WorkspaceRegeneratePanel.tsx`

最直接接入：

- 当前选中 slide
- 修改描述
- 调用 `/api/regenerate-teaching`
- 回写新的 `TeachingDesign`
- 再重新跑 workspace mapper

### 2. `teaching-design-session.ts`

第三步可升级为：

- 支持更新 draft
- 支持再生成后的 design 覆盖写回 session

### 3. `teaching-design-to-workspace.ts`

第三步可继续增强：

- 当前页 evidence 聚焦
- 局部变更状态
- slide 级“待更新 / 已更新”状态
- 更细的 workflow steps

### 4. intent 页 mapper

第三步如果接真实多轮 agent，可把：

- 当前手工 messages 压缩逻辑 升级为：
- 会话结构化结果优先
- 手工补充为 fallback

------

## 本阶段结果总结

这次已经完成了你要求的第二步核心目标：

- 意图理解页接入真实 PDF 解析
- 生成按钮接入真实教学设计生成接口
- workspace 能读取并展示真实 `TeachingDesign`
- 新增 mapper 层，UI 不直接吃业务对象
- 继续保留现有页面结构
- 无 regenerate、无多轮 agent、无持久化、无 Stage 改动

如果你愿意，我下一步可以继续做两件事之一：

1. 先帮你做一次代码复盘，找出第二步里最该收敛的小问题
2. 按你的计划，直接进入第三步的具体实现指令