# 三源融合增强版 - 快速开始指南

## 🚀 5分钟快速体验

### 步骤 1: 准备测试数据

#### 1.1 创建测试用的教学需求
```typescript
const testRequest: TeachingRequest = {
  subject: "计算机科学",
  topic: "操作系统的进程管理",
  gradeLevel: "大学本科",
  duration: 45,
  objectives: {
    knowledge: [
      "理解进程的基本概念",
      "掌握进程的状态转换",
      "了解进程调度算法"
    ],
    skills: [
      "能够分析进程状态转换过程",
      "能够比较不同调度算法的优缺点"
    ]
  },
  useKnowledgeBase: true,
  language: "zh-CN"
};
```

#### 1.2 准备参考资料
- 上传一个包含"进程管理"内容的 PDF 文件
- 或使用测试 PDF（包含进程概念、状态图等）

#### 1.3 确保知识库已配置
- 检查 `.env.local` 中的 FastGPT 配置
- 确保知识库中有相关内容

### 步骤 2: 生成教学设计

```typescript
// 在测试页面或 API 中调用
const result = await generateTeachingDesignFromRequest(
  testRequest,
  materials,
  aiCall,
  callbacks
);

if (result.success && result.data) {
  console.log('生成成功！');
  console.log('总页数:', result.data.slides.length);
}
```

### 步骤 3: 查看来源统计

打开浏览器控制台，查找以下日志：

```
[TeachingGeneration] Three-source fusion statistics: {
  materialUsage: 8,
  ragUsage: 7,
  teacherUsage: 5,
  totalItems: 20,
  materialPercentage: '40.0%',
  ragPercentage: '35.0%',
  teacherPercentage: '25.0%'
}
```

### 步骤 4: 检查生成结果

```typescript
// 查看第一页的 keyPoints
const firstSlide = result.data.slides[0];
console.log('第一页标题:', firstSlide.title);

firstSlide.keyPoints.forEach((kp, index) => {
  console.log(`要点 ${index + 1}:`, kp.content);
  console.log(`来源:`, kp.source);
  console.log('---');
});
```

预期输出：
```
要点 1: 进程是程序的一次执行过程，是系统进行资源分配和调度的基本单位
来源: material
---
要点 2: 进程具有动态性、并发性、独立性、异步性四个基本特征
来源: knowledge
---
要点 3: 理解进程与程序的区别是本节课的重点
来源: teacher
---
```

---

## 📊 使用来源追踪工具

### 生成统计报告

```typescript
import { calculateSourceUsageStats } from '@/lib/generation/teaching-outline-generator';
import { generateSourceUsageReport } from '@/lib/generation/source-tracker';

// 计算统计
const stats = calculateSourceUsageStats(design);

// 生成报告
const report = generateSourceUsageReport(stats);
console.log(report);
```

输出：
```
=== 三源融合统计报告 ===

总内容项数: 20

来源分布:
  📄 参考资料: 8 项 (40.0%)
  📚 知识库: 7 项 (35.0%)
  👨‍🏫 教师需求: 5 项 (25.0%)

约束检查:
  参考资料 ≥ 30%: ✅ 满足
  知识库 ≥ 30%: ✅ 满足

========================
```

### 验证来源分布

```typescript
import { validateSourceDistribution } from '@/lib/generation/source-tracker';

const validation = validateSourceDistribution(stats);

if (validation.valid) {
  console.log('✅ 来源分布满足约束');
} else {
  console.warn('⚠️ 来源分布不满足约束:');
  validation.issues.forEach(issue => {
    console.warn('  -', issue);
  });
}
```

### 格式化显示知识点

```typescript
import { formatKeyPointWithSource } from '@/lib/generation/source-tracker';

design.slides.forEach((slide, index) => {
  console.log(`\n第 ${index + 1} 页 - ${slide.title}`);
  slide.keyPoints.forEach(kp => {
    console.log('  ' + formatKeyPointWithSource(kp));
  });
});
```

输出：
```
第 1 页 - 进程的基本概念
  📄 进程是程序的一次执行过程，是系统进行资源分配和调度的基本单位 [来源: 参考资料]
  📚 进程具有动态性、并发性、独立性、异步性四个基本特征 [来源: 知识库]
  👨‍🏫 理解进程与程序的区别是本节课的重点 [来源: 教师需求]
```

---

## 🔍 调试技巧

### 1. 查看完整的上下文

在生成前，可以打印 `mergedContext`：

```typescript
const contextBundle = buildTeachingContextBundle(request, parsedMaterials, ragContext);
console.log('=== 合并后的上下文 ===');
console.log(contextBundle.mergedContext);
```

### 2. 检查 RAG 查询

```typescript
const query = buildKnowledgeQueryFromTeachingRequest(request);
console.log('=== RAG 查询 ===');
console.log(query);
```

### 3. 查看原始 LLM 响应

在 `generateTeachingDesignFromRequest` 中添加：

```typescript
const response = await aiCall(systemPrompt, userPrompt, visionImages);
console.log('=== LLM 原始响应 ===');
console.log(response.substring(0, 500)); // 前500字符
```

### 4. 验证 JSON 解析

```typescript
const designData = parseJsonResponse<Partial<TeachingDesign>>(response);
console.log('=== 解析后的数据 ===');
console.log('slides 数量:', designData.slides?.length);
console.log('第一页 keyPoints:', designData.slides?.[0]?.keyPoints);
```

---

## ⚠️ 常见问题

### Q1: 所有 keyPoints 都没有 source 字段

**原因**: LLM 可能没有遵循 prompt 要求

**解决**:
1. 检查 system prompt 是否正确传递
2. 尝试更强的模型（如 GPT-4）
3. 增加 prompt 中的示例

### Q2: 来源分布不均衡（某个来源 < 30%）

**原因**: 
- 参考资料内容不足
- 知识库查询失败
- LLM 偏好某个来源

**解决**:
1. 检查参考资料是否成功解析
2. 检查 RAG 查询是否返回内容
3. 调整 prompt 中的比例要求（如改为 25%）

### Q3: source 字段值不正确

**原因**: LLM 返回了非预期的值

**解决**:
添加后处理规范化：

```typescript
slides.forEach(slide => {
  slide.keyPoints = slide.keyPoints.map(kp => {
    if (typeof kp === 'string') {
      return { content: kp, source: undefined };
    }
    // 规范化 source 值
    if (kp.source && !['teacher', 'material', 'knowledge'].includes(kp.source)) {
      kp.source = undefined;
    }
    return kp;
  });
});
```

### Q4: 统计日志没有输出

**原因**: 日志级别设置问题

**解决**:
```typescript
import { createLogger } from '@/lib/logger';
const log = createLogger('TeachingGeneration');
log.setLevel('info'); // 确保日志级别
```

---

## 🎯 最佳实践

### 1. 准备高质量的输入

✅ **好的参考资料**:
- 内容详细，有专业术语
- 包含图表、公式
- 结构清晰

❌ **不好的参考资料**:
- 内容过于简单
- 只有标题没有正文
- 格式混乱

### 2. 配置好知识库

✅ **好的知识库**:
- 包含相关领域的专业知识
- 内容经过整理和标注
- 定期更新

❌ **不好的知识库**:
- 内容过于泛化
- 与教学主题无关
- 长时间未更新

### 3. 明确教学目标

✅ **好的教学目标**:
```typescript
objectives: {
  knowledge: [
    "理解进程的基本概念和特征",  // 具体
    "掌握进程状态转换的触发条件"  // 明确
  ]
}
```

❌ **不好的教学目标**:
```typescript
objectives: {
  knowledge: [
    "了解进程",  // 过于笼统
    "学习操作系统"  // 范围太大
  ]
}
```

### 4. 验证生成结果

每次生成后，检查：
- [ ] 来源统计是否满足约束
- [ ] 内容质量是否符合预期
- [ ] 三种来源是否都有使用
- [ ] 专业术语是否准确

---

## 📚 进阶使用

### 自定义来源权重

虽然当前版本使用固定的 30% 约束，但可以通过修改 prompt 来调整：

```typescript
// 在 teaching-context-builder.ts 中修改
sections.push('1. **至少 40% 内容来自【参考资料】**');  // 从 30% 改为 40%
sections.push('2. **至少 25% 内容来自【知识库】**');    // 从 30% 改为 25%
```

### 添加来源详情

在生成时要求 LLM 提供更详细的来源信息：

```typescript
{
  "content": "进程是资源分配的基本单位",
  "source": "material",
  "sourceDetail": "参考资料第3章第1节"  // 详细引用
}
```

### 实现来源过滤

```typescript
// 只显示来自参考资料的内容
const materialSlides = design.slides.map(slide => ({
  ...slide,
  keyPoints: slide.keyPoints.filter(kp => kp.source === 'material')
})).filter(slide => slide.keyPoints.length > 0);
```

---

## 🎓 学习资源

- **实施报告**: `三源融合增强版实施报告.md` - 详细的技术实现
- **测试示例**: `三源融合增强版测试示例.md` - 完整的测试案例
- **Prompt 对比**: `三源融合Prompt对比.md` - 修改前后对比
- **交付清单**: `三源融合增强版_交付清单.md` - 完整的交付内容

---

## 💡 小贴士

1. **首次使用**: 建议使用提供的测试示例，熟悉整个流程
2. **调试时**: 打开浏览器控制台，查看详细日志
3. **遇到问题**: 先检查输入数据是否完整，再检查配置
4. **优化效果**: 多次测试，调整 prompt 和输入数据

---

**祝你使用愉快！** 🎉

如有问题，请查看详细文档或联系开发团队。
