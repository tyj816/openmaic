# DOCX 参考资料支持指南

## 概述

OpenMAIC 的三源融合系统现已支持上传 Word 文档（DOCX/DOC）作为参考资料，与 PDF 和图片一起用于教学设计生成。

## 功能特性

### 支持的文件格式
- `.docx` - Office Open XML 格式（Word 2007+）
- `.doc` - 传统 Word 文档格式

### 提取能力
1. **文本内容**：完整提取文档中的所有文本
2. **嵌入图片**：自动提取文档中的图片（base64 格式）
3. **文件元数据**：文件名、大小、图片数量

## 使用方法

### 1. 上传 DOCX 文件

在教学设计测试页面（`/teaching-test`）：

```
1. 点击"上传 PDF、Word 或图片"按钮
2. 选择 .docx 或 .doc 文件
3. 系统自动解析并显示在资料列表中
```

### 2. 资料显示

上传成功后，资料列表会显示：
- 📝 图标（表示 Word 文档）
- 文件名
- 提取的图片数量
- 删除按钮

### 3. 生成教学设计

DOCX 内容会自动融入三源系统：
- 文本内容 → 合并到参考资料上下文
- 嵌入图片 → 可在课件中引用
- 与 PDF、图片、知识库内容一起生成

## 技术实现

### API 端点

```
POST /api/parse-docx
Content-Type: multipart/form-data

Request:
{
  docx: File
}

Response:
{
  success: true,
  data: {
    text: string,           // 提取的文本内容
    images: ParsedImage[],  // 提取的图片数组
    metadata: {
      fileName: string,
      fileSize: number,
      imageCount: number
    }
  }
}
```

### 使用的库

- **mammoth** (v1.12.0)
  - `extractRawText()` - 提取纯文本
  - `convertToHtml()` - 转换为 HTML 以提取图片

### 数据流

```
DOCX 文件
  ↓
/api/parse-docx
  ↓ mammoth.extractRawText()
  ↓ mammoth.convertToHtml()
  ↓
{ text, images, metadata }
  ↓
ReferenceMaterial (type: 'docx')
  ↓
parseTeachingMaterials()
  ↓
TeachingContextBundle
  ↓
生成教学设计
```

## 示例

### 输入：教学大纲.docx

```
文档内容：
---
课程名称：毛泽东思想概论
教学目标：
1. 理解实事求是的核心地位
2. 掌握群众路线的基本内涵
3. 认识独立自主的重要意义

重点难点：
- 重点：实事求是的理论与实践
- 难点：如何在实际工作中贯彻群众路线

[图片：毛泽东思想体系图]
---
```

### 解析结果

```typescript
{
  text: "课程名称：毛泽东思想概论\n教学目标：\n1. 理解实事求是的核心地位...",
  images: [
    {
      id: "docx_img_0",
      src: "data:image/png;base64,iVBORw0KG...",
      description: "DOCX 文档中的图片 1"
    }
  ],
  metadata: {
    fileName: "教学大纲.docx",
    fileSize: 45678,
    imageCount: 1
  }
}
```

### 融入生成

生成的 prompt 会包含：

```markdown
# 【参考资料内容】

## 【教学大纲.docx】
课程名称：毛泽东思想概论
教学目标：
1. 理解实事求是的核心地位
2. 掌握群众路线的基本内涵
...

# 【参考资料中的图片/图示资源】
共有 1 张可用图片，可在课件中引用
```

## 优势

### 1. 教师友好
- Word 是教师最常用的文档格式
- 无需转换，直接上传即可使用
- 保留原有的教学大纲和备课资料

### 2. 内容丰富
- 提取完整的文本内容
- 保留文档中的图片资源
- 支持复杂的文档结构

### 3. 无缝集成
- 与 PDF、图片处理方式一致
- 自动融入三源融合流程
- 不影响现有功能

## 限制与注意事项

### 当前限制
1. **格式支持**：仅支持文本和图片，不支持表格、图表等复杂元素
2. **样式丢失**：提取纯文本，不保留格式（字体、颜色等）
3. **图片质量**：图片以 base64 嵌入，可能增加数据大小

### 最佳实践
1. **文档准备**：
   - 使用清晰的标题和段落结构
   - 重要内容使用文本而非图片
   - 图片清晰且有意义

2. **文件大小**：
   - 建议单个文件 < 10MB
   - 图片过多可能影响解析速度
   - 可以分多个文件上传

3. **内容组织**：
   - 一个文档聚焦一个主题
   - 使用明确的章节标题
   - 避免过于复杂的嵌套结构

## 错误处理

### 常见错误

1. **文件格式错误**
   ```
   错误：DOCX parsing failed
   原因：文件损坏或格式不正确
   解决：重新保存文档或转换格式
   ```

2. **文件过大**
   ```
   错误：File size exceeds limit
   原因：文件超过大小限制
   解决：压缩图片或拆分文档
   ```

3. **解析失败**
   ```
   错误：Failed to extract content
   原因：文档包含不支持的元素
   解决：简化文档内容，移除复杂元素
   ```

### 降级策略

如果 DOCX 解析失败：
- 系统会记录错误日志
- 继续处理其他资料
- 不影响整体生成流程
- 用户会收到友好的错误提示

## 测试验证

### 快速测试

1. 创建测试文档 `test.docx`：
   ```
   毛泽东思想的活的灵魂
   
   三个基本方面：
   1. 实事求是
   2. 群众路线
   3. 独立自主
   ```

2. 上传到 `/teaching-test`

3. 检查资料列表是否显示 📝 图标

4. 生成教学设计，验证内容是否包含文档内容

### 完整测试

参考《三源融合闭环实施报告》第七节"最小测试方案"，使用 DOCX 文件替代 PDF 进行测试。

## 未来扩展

### 计划功能
- [ ] 支持表格提取
- [ ] 支持图表识别
- [ ] 保留部分格式（标题层级）
- [ ] 支持批注和修订
- [ ] 智能章节识别

### 性能优化
- [ ] 大文件分块处理
- [ ] 图片压缩和优化
- [ ] 缓存解析结果
- [ ] 并行处理多个文件

## 相关文档

- [三源融合闭环实施报告](./三源融合闭环实施报告.md)
- [FastGPT 集成指南](./FASTGPT_INTEGRATION_GUIDE.md)
- [教学设计生成指南](./TEACHING_REGENERATOR_GUIDE.md)

## 技术支持

如遇问题，请检查：
1. 浏览器控制台日志
2. 服务器日志（`[Parse DOCX]` 标签）
3. 文件格式和大小
4. mammoth 库版本

---

更新时间：2024
版本：v1.0
