# 客户端/服务端分离修复报告

## 🚨 问题描述

**错误信息**：
```
Module not found: Can't resolve 'fs'
```

**根本原因**：
- `use-teaching-generator.ts` 是客户端 hook，但直接导入了服务端代码
- `teaching-slide-generator.ts` 导入了 `prompts` 系统
- `prompts/loader.ts` 使用了 Node.js 的 `fs` 模块
- Next.js 不允许在客户端代码中使用 Node.js 模块

## 🔧 解决方案

### 架构调整：客户端 → API → 服务端

**修改前**：
```
客户端 Hook → 直接调用服务端函数 → 使用 fs 模块 ❌
```

**修改后**：
```
客户端 Hook → API 端点 → 服务端函数 → 使用 fs 模块 ✅
```

## 📁 新增文件

### 1. `/api/generate/teaching-outline/route.ts`
- 处理教学大纲生成的 API 端点
- 接收 `TeachingRequest` 和模型配置
- 调用 `generateTeachingDesignFromRequest`

### 2. `/api/generate/teaching-slide/route.ts`
- 处理单个课件页面生成的 API 端点
- 接收 `TeachingSlide` 和模型配置
- 调用 `generateSlideFromTeachingSlide`

## 🔄 修改文件

### 1. `lib/hooks/use-teaching-generator.ts`

**修改前**：
```typescript
// 直接导入服务端代码
import { generateTeachingDesignFromRequest } from '@/lib/generation/teaching-outline-generator';
import { generateSlideFromTeachingSlide } from '@/lib/generation/teaching-slide-generator';

// 直接调用服务端函数
const designResult = await generateTeachingDesignFromRequest(...);
const canvas = await generateSlideFromTeachingSlide(...);
```

**修改后**：
```typescript
// 通过 API 调用
const outlineResponse = await fetch('/api/generate/teaching-outline', {
  method: 'POST',
  body: JSON.stringify({ request, modelString, apiKey, ... })
});

const slideResponse = await fetch('/api/generate/teaching-slide', {
  method: 'POST', 
  body: JSON.stringify({ slide, modelString, apiKey, ... })
});
```

### 2. `app/teaching-test/page.tsx`

**修改前**：
```typescript
const design = await generator.generate(request, {
  model,
  modelId: 'glm-5', // 简单字符串
  visionEnabled: false,
});
```

**修改后**：
```typescript
const design = await generator.generate(request, {
  model,
  modelString: 'glm/glm-5', // 完整模型字符串
  apiKey: modelConfig.apiKey,
  baseUrl: modelConfig.baseUrl,
  providerType: modelConfig.providerType,
  requiresApiKey: modelConfig.requiresApiKey,
  visionEnabled: false,
});
```

## 🔧 模型解析修复

### 问题
原来的 API 端点错误地调用了 `resolveModel(modelId)`，但 `resolveModel` 函数期望的是一个配置对象。

### 修复
```typescript
// 修改前
const model = await resolveModel(modelId); // ❌ 错误用法

// 修改后  
const { model } = resolveModel({
  modelString,
  apiKey,
  baseUrl,
  providerType,
  requiresApiKey,
}); // ✅ 正确用法
```

## 📊 数据流对比

### 修改前（有问题）
```
客户端页面
    ↓ 直接调用
客户端 Hook
    ↓ 直接导入
服务端生成函数
    ↓ 导入
Prompts 系统 (fs 模块) ❌ 客户端无法访问
```

### 修改后（正常）
```
客户端页面
    ↓ 调用
客户端 Hook
    ↓ HTTP 请求
API 端点 (/api/generate/*)
    ↓ 调用
服务端生成函数
    ↓ 导入
Prompts 系统 (fs 模块) ✅ 服务端正常访问
```

## ✅ 验证结果

1. **编译成功**：无 `fs` 模块错误
2. **类型检查通过**：所有 TypeScript 类型正确
3. **服务器启动正常**：`http://localhost:3000` 可访问
4. **API 端点就绪**：
   - `POST /api/generate/teaching-outline`
   - `POST /api/generate/teaching-slide`

## 🎯 关键改进

1. **架构清晰**：客户端和服务端职责分离
2. **类型安全**：完整的 TypeScript 类型定义
3. **错误处理**：API 层面的错误处理和状态管理
4. **可扩展性**：新的 API 结构便于后续功能扩展

## 🚀 下一步

现在可以正常访问 `http://localhost:3000/teaching-test` 并测试：
1. 填写教学信息
2. 点击"生成教学设计"
3. 验证 PPT 质量恢复效果

---

*修复完成时间：2026年4月5日*  
*涉及文件：6个（新增2个，修改4个）*