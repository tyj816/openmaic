# 教学系统改造方案（基于 OpenMAIC 原生工作流复用）

## 1. 背景与目标

当前项目已经具备较完整的教学设计链路基础：

- 教师需求输入与多轮对话澄清
- 参考资料上传与解析
- 本地知识库 RAG（FastGPT）接入与验证
- 三源融合：教师需求 + 参考资料 + 知识库检索
- 教学设计工作台预览
- PPT / DOCX 导出
- 页级再生成能力雏形

但从实际生成效果看，当前教学系统导出的 PPT 质量仍明显弱于 OpenMAIC 原生流程，主要表现为：

- 页面信息密度偏低
- 页面布局杂乱，视觉完成度不足
- 页与页之间缺少完整课程级叙事节奏
- 虽然“教学设计结构”较完整，但最终 PPT 不够“成品化”

因此，本次改造的核心目标不是重做整个系统，而是：

> **保留现有“三源融合教学系统”框架，将底层 PPT 生成引擎切换为 OpenMAIC 原生 scene workflow 的裁剪版，从而显著提升课件成品质量，同时满足赛题要求。**

---

## 2. 赛题要求与当前实现映射

## 2.1 已基本具备的能力

### （1）本地知识库 RAG

当前已完成：

- FastGPT 本地知识库接入
- RAG 接口联调与测试
- 检索结果在教学设计生成中参与使用
- quoteList / ragChunks 等结构化结果已具备后续证据追踪基础

结论：

- 赛题第 1 部分已具备较强基础
- 后续重点不是“有没有 RAG”，而是“如何把 RAG 的结果更好融入 PPT 页面生成，并形成可视化证据链”

### （2）多轮需求澄清与资料上传

当前已具备或已有前端基础：

- 教师文字输入
- 教学思路结构化理解
- 资料上传入口与 UI 工作台
- 对话式需求确认的产品方向明确

结论：

- 视频解析和语音输入暂不作为当前阶段硬要求
- 当前版本可聚焦“文字输入 + 多轮澄清 + 多格式资料上传”

### （3）教学意图理解与三源融合

当前已经实现：

- 教师需求结构化抽取
- 参考资料文本与图片解析
- FastGPT 检索增强
- 三源融合上下文构建
- keyPoints 与 source / ragChunkId 的来源追踪

结论：

- 赛题第 3 部分已经是项目优势模块
- 应继续保留，并作为整体方案中的“上游能力层”

### （4）工作台预览、导出、再生成

当前已经具备：

- 教学设计工作台 UI
- PPT 预览 / DOCX 预览切换
- PPT / DOCX 导出
- 单页再生成能力雏形
- 引用与证据展示区

结论：

- 赛题第 5 部分已有基础实现
- 重点是增强结构化修改与重生成体验，而不是推翻重做

---

## 2.2 当前最主要短板

项目当前最大的不足不在“赛题功能覆盖率”，而在：

# **多模态课件生成引擎中的 PPT 生成质量**

也就是赛题第 4 部分中的核心产出质量。

当前教学系统采用的流程大致为：

```text
教师需求 / 参考资料 / 知识库
→ TeachingDesign
→ TeachingSlide
→ 单页 canvas 生成
→ 导出 PPT
```

该模式本质上更接近：

> “先做教案摘要，再逐页转成 PPT”

其问题是：

- 单页上下文不足
- 课程整体叙事弱
- 页面之间缺少全局约束
- 每页虽然有内容，但成品观感不够稳定

而 OpenMAIC 原生流程更接近：

```text
需求
→ scene outlines
→ scene content
→ scene assemble
→ slide / PPT
```

该流程天然更适合生成“完整课程级成品 PPT”。

---

## 3. 改造总体思路

本次建议采用：

# **“三源融合不变，PPT 生成内核升级”方案**

即：

### 保留不动的部分

1. 教师需求输入与多轮澄清
2. 参考资料上传与解析
3. FastGPT 本地知识库 RAG
4. 三源融合与证据链设计
5. 教学设计工作台 UI
6. PPT / DOCX 导出能力
7. 页级局部再生成产品形态

### 重点替换的部分

1. 教学设计生成后的 PPT 生成主链
2. 从 `TeachingSlide -> teaching-slide-generator` 的孤立单页生成模式
3. 改为复用 OpenMAIC 原生 `scene workflow` 的核心链路

一句话概括：

> **保留教学系统前半段，把后半段 PPT 生成内核换成 OpenMAIC 原生高质量生成引擎的裁剪版。**

---

## 4. 推荐的新架构

## 4.1 总体分层

建议将系统拆成四层：

### 第一层：输入层
负责收集并组织教学输入。

包括：

- 教师文字输入
- 多轮澄清对话
- 参考资料上传
- PDF / Word / PPT / 图片解析
- 本地知识库 RAG 查询

### 第二层：教学融合层
负责把多源信息融合为统一的生成规格。

建议新增统一对象：

`TeachingGenerationSpec`

包含字段建议：

- 学科
- 年级
- 课题
- 课时
- 教学目标
- 重点难点
- 逻辑顺序
- 页数范围
- 风格偏好
- 引用资料片段
- 知识库证据片段
- 建议用图页
- 是否允许 quiz / 动画 / 互动页
- 禁止项（如不需要语音讲解、白板、PBL）

### 第三层：PPT 生成层
改造重点。

由 `TeachingGenerationSpec` 驱动 OpenMAIC 原生 scene 工作流的裁剪版：

- scene outlines
- scene content
- 轻量 scene assemble
- canvas / slide build

### 第四层：工作台与导出层
保留现有教学系统产品形态，将 scene 结果映射回：

- `TeachingDesign`
- `WorkspaceViewModel`
- DOCX 导出数据结构
- 证据链展示模型

---

## 4.2 改造后主流程

建议新流程如下：

```text
教师需求 + 参考资料 + FastGPT 知识库
→ 教学意图理解与三源融合
→ 生成 TeachingGenerationSpec
→ Scene Outlines（课程全局结构）
→ Scene Content（单页内容结构）
→ Light Scene Assemble（不做语音/课堂互动）
→ 回填为 TeachingDesign
→ 工作台预览 / 再生成 / 导出
```

关键变化在于：

### 旧模式
`TeachingDesign` 是生成源头

### 新模式
`TeachingDesign` 变成展示与编辑视图

也就是说：

> **底层成品 PPT 的生成主逻辑不再直接由 TeachingDesign 驱动，而由 Scene Workflow 驱动；TeachingDesign 负责承接与呈现结果。**

---

## 5. 为什么推荐复用 OpenMAIC 原生工作流

## 5.1 原生工作流的优势

OpenMAIC 原生流程的 PPT 质量更高，不只是因为 prompt 写得长，而是因为它具备完整的课程级生成链路：

1. **Outlines 先决定全局结构**
   - 页数
   - 页型
   - 节奏
   - quiz 插入位置
   - 每页职责

2. **Content 再决定单页内容形态**
   - 标题
   - 要点密度
   - 图文搭配
   - 元素结构

3. **Scene assemble 保证结果更接近成品**
   - 页面具备完整课程上下文
   - 不只是“单页排版”，而是“整课构成”

4. **Prompt / post-process 已经成熟**
   - slide-content system prompt 非常完整
   - fix defaults / latex / image resolve 等后处理逻辑成熟
   - 页面成品稳定性更高

## 5.2 当前教学系统的问题

当前教学系统虽已复用部分 `slide-content` prompt，但只是在：

- 单页孤立输入
- 单页独立生成 canvas
- 缺少 course-level scene context

因此会导致：

- 内容少
- 页面散
- 结构弱
- 美观度不足

所以最优路线不是继续单点打磨 `teaching-slide-generator`，而是：

> **把它放回原生 scene pipeline 的更完整上下文里。**

---

## 6. 具体保留与裁剪建议

## 6.1 应保留复用的部分

### （1）Scene Outlines 层
必须复用。

原因：

- 决定课程页序与节奏
- 决定封面/目录/内容/总结页结构
- 决定哪些页适合 quiz
- 决定哪些页需要图片
- 决定整体课程叙事感

### （2）Scene Content 层
必须复用。

原因：

- 它承担“单页内容生成”的主体工作
- 比当前 `TeachingSlide -> canvas` 方式更完整
- 能显著提升页面内容组织质量

### （3）Slide Content Prompt 体系
继续复用。

原因：

- 这是 OpenMAIC 高质量页面生成的关键能力之一
- 目前项目已经局部使用，但使用方式不完整
- 应放回原生 scene workflow 上下文中使用

### （4）现有导出层
继续保留。

原因：

- 当前已有 `TeachingDesign -> Scene[] -> PPTX` 适配器
- DOCX 导出链路已有基础
- 可避免导出层大规模重构

---

## 6.2 应裁剪或降级的部分

### （1）TTS
建议直接关闭。

原因：

- 与比赛版 PPT 成品质量无直接关系
- 属于课堂播放演示层功能
- 增加耗时与失败点

### （2）智能体互动 / Agent Profiles
建议关闭或降级为空上下文。

原因：

- 对页面成品质量并非核心必要项
- 主要影响课堂演示感、角色互动、语音风格
- 与本项目“教师课件生成”主任务不完全匹配

### （3）PBL / 白板讲解 / 重互动课堂模块
建议关闭。

原因：

- 与当前比赛阶段目标不匹配
- 开发复杂度高
- 容易分散主线精力

### （4）复杂 interactive 类型
建议先关闭，仅保留一种轻量互动能力用于满足赛题“内容生成多样性”要求。

推荐保留：

- `quiz` 或轻量互动测验页

不建议当前阶段保留：

- 复杂 HTML5 模拟器
- 项目式 PBL 大互动
- 白板演示链路

---

## 7. 内容多样性建议（满足赛题要求）

赛题要求课件引擎具备一定内容生成多样性，至少支持一种动画创意、互动小游戏等能力。

考虑当前时间与主任务优先级，建议采用：

# **“轻量 quiz / 互动测验页” 作为比赛版达标能力**

原因：

1. 与教学场景天然契合
2. 已与原生 scene 流程兼容度较高
3. 比复杂互动页开发成本低很多
4. 更容易集成到 PPT / DOCX / 工作台中
5. 在答辩演示中也更直观

建议范围：

- 单选题
- 判断题
- 简单配对/归类（如后续有余力）

结论：

- 比赛版先支持 quiz，即可满足“至少一种”多样性要求
- 不建议当前阶段扩展为大型互动模块

---

## 8. 证据链与赛题亮点设计

为了突出“本地知识库 + 三源融合”的专业性，建议在改造中强化页级证据链。

## 8.1 证据链应包含三类来源

1. 教师需求
2. 上传资料
3. 知识库检索结果

## 8.2 页面级证据展示建议

在工作台中，针对当前页展示：

- 该页引用了哪些教师要求
- 该页参考了哪些资料片段 / 图片
- 该页引用了哪些 RAG chunk
- 哪些内容来自知识库，哪些来自教师意图，哪些来自资料

## 8.3 比赛答辩价值

这样可以把系统从“普通文生 PPT”提升为：

> **可追溯、可解释、基于专业知识库与教学资料融合的教学智能体系统**

这会显著提升方案说服力。

---

## 9. 分阶段实施路线

为了控制风险和开发成本，建议按三个阶段推进。

## 第一阶段：比赛可交付优先（最关键）

### 目标

快速验证并落地“PPT 质量明显提升”的主链改造。

### 主要任务

1. 保留现有三源融合上游流程
2. 新建一条 `scene-based teaching generation` 链路
3. 将 `TeachingGenerationSpec` 转为 `scene outline` 输入
4. 复用：
   - scene outlines
   - scene content
   - 轻量 assemble
5. 关闭：
   - TTS
   - agent profiles
   - PBL
   - whiteboard
6. 限制 scene type：
   - `slide`
   - 可选 `quiz`
7. 将 scene 结果映射回 `TeachingDesign`
8. 保持工作台 UI 基本不变

### 第一阶段成果标准

- 同样模型、同样资料条件下，PPT 成品质量明显优于当前教学系统流程
- 稳定生成封面 / 目录 / 内容 / 总结页
- 支持 PPT / DOCX 导出
- 支持工作台预览

---

## 第二阶段：完善教学工作台与修改能力

### 目标

让系统具备更完整的教学编辑体验。

### 建议增强

1. 单页再生成能力增强
   - 简化某页
   - 增加案例
   - 增加一张总结页
   - 更适合某年龄段

2. 结构修改能力
   - 调整顺序
   - 新增某一页
   - 删除冗余页

3. 全局风格修改
   - 更简洁
   - 更活泼
   - 图更多、字更少

4. 证据链面板增强
   - 页级来源高亮
   - 资料片段与 RAG chunk 可追踪

### 第二阶段成果标准

- 教师可在工作台内进行结构性修改
- 修改后重新生成结果能保持稳定质量
- 证据链展示更清晰

---

## 第三阶段：补充赛题亮点能力

### 目标

在主链稳定后补强展示亮点。

### 可选项

1. quiz 样式升级
2. 轻量 HTML5 互动页
3. 语音输入预留接口
4. 视频资料解析预研
5. 更强的课堂活动设计导出

### 原则

- 只在主链质量稳定后再做
- 不要抢占第一阶段的核心目标

---

## 10. 技术改造建议（模块级）

以下为建议的模块级调整方向。

## 10.1 建议保留的现有模块

### 上游能力模块

保留：

- `lib/generation/teaching-outline-generator.ts` 中的三源融合逻辑
- `lib/generation/teaching-material-parser.ts`
- `lib/generation/teaching-context-builder.ts`
- `lib/ai/fastgpt-client.ts`
- 意图理解与对话相关页面和数据结构

原因：

- 这些模块代表项目的比赛特色与专业能力
- 不应因更换 PPT 引擎而推翻

### 工作台与导出模块

保留：

- `app/teaching-design/workspace/page.tsx`
- `components/teaching-design/workspace/*`
- `lib/export/use-export-teaching-pptx.ts`
- `app/api/generate-docx/route.ts`
- `lib/generation/teaching-regenerator.ts`（后续适配加强）

原因：

- 它们是现有产品表现层基础
- 替换成本高，且非当前瓶颈

---

## 10.2 建议新增的核心模块

### （1）TeachingGenerationSpec 构建器

建议新增：

- `lib/generation/teaching-generation-spec.ts`

职责：

- 将教师需求、资料解析、RAG 结果统一组织成 scene workflow 可消费的规格对象

### （2）Teaching -> Scene 输入适配器

建议新增：

- `lib/adapters/teaching-to-scene-input.ts`

职责：

- 把 `TeachingGenerationSpec` 转成 outlines prompt 所需变量
- 明确禁止项和允许项
- 把页型约束固定下来

### （3）Scene -> TeachingDesign 输出适配器

建议新增：

- `lib/adapters/scene-to-teaching-design.ts`

职责：

- 把生成后的 scenes / slide content 映射回 `TeachingDesign`
- 保留 source / ragChunkId / materials 关联
- 供工作台和 DOCX 使用

### （4）精简版 teaching scene pipeline hook

建议新增：

- `lib/hooks/use-teaching-scene-generator.ts`

职责：

- 替代当前 `useTeachingGenerator` 的主生成逻辑
- 不再逐页调用 `teaching-slide`
- 改为调用 outlines + scene-content + assemble 的精简链路

---

## 10.3 建议弱化或逐步退出的模块

### （1）`teaching-slide-generator.ts`

建议定位调整为：

- 备用单页重画工具
- 局部再生成工具
- 不再作为主生成流程核心

原因：

- 它适合“单页修补”，不适合承担整课生成主链

### （2）当前 `useTeachingGenerator`

建议逐步替换其主职责。

原因：

- 当前串行逐页生成模式是 PPT 质量瓶颈之一
- 应迁移到新的 scene-based pipeline

---

## 11. 推荐的最小改造路径

为了尽快落地，建议采用最小可行改造法。

## 第一步：先做旁路验证

不要直接推翻现有主流程，先新增一条实验链路：

- 输入仍然来自当前教学系统
- 输出改走精简 scene workflow
- 生成结果单独落到工作台或测试入口

目的：

- 快速比较新旧 PPT 成品质量
- 控制风险
- 不影响当前已有演示链路

## 第二步：固定页型与禁用项

比赛版先强约束：

- 必有：封面、目录、内容页、总结页
- 可选：quiz
- 禁止：agent / TTS / PBL / whiteboard /复杂互动

目的：

- 保证结果稳定
- 保证答辩时展示效果统一

## 第三步：映射回现有工作台

不要重做工作台，而是：

- 将新 scene 输出适配回 `TeachingDesign`
- 让工作台继续消费 `TeachingDesign`

目的：

- 用最小成本完成生成内核替换
- 最大化复用现有 UI 与导出能力

---

## 12. 风险与应对策略

## 风险 1：切换 scene workflow 后，类型不受控

### 表现
- outline 生成出不需要的 interactive / pbl 页面

### 应对
- 在 prompt 中明确限制类型白名单
- 在后端加 schema 校验和 fallback
- 比赛版只允许 `slide` 与可选 `quiz`

## 风险 2：关闭 agent 后质量略有变化

### 表现
- 页面演示感可能略弱

### 应对
- 将教学上下文直接注入 outline / content prompt
- 强化教师 persona / 课程语气说明
- 用教学目标和页型约束替代 agent 作用

## 风险 3：局部再生成与新主链不兼容

### 表现
- 工作台中修改某页后不易保持整体一致性

### 应对
- 第一阶段先支持页级局部改写
- 后续再增强顺序级 / 全局级修改
- 保留旧单页生成器作为补丁式再生成工具

## 风险 4：证据链在 scene 输出中丢失

### 表现
- 页面内容来源变得不可追踪

### 应对
- 在 scene 输出适配回 TeachingDesign 时补充来源元数据
- 在 `scene-to-teaching-design` 中建立 source mapping
- 保留 ragChunkId / sourceDetail / material refs

---

## 13. 比赛版优先级建议

## P0（必须优先完成）

1. Scene-based PPT 生成新主链
2. 固定页型输出：封面 / 目录 / 内容 / 总结
3. 保留三源融合证据链
4. 保留 PPT / DOCX 导出
5. 工作台正常预览新结果

## P1（建议完成）

1. quiz 作为一种内容多样性能力
2. 单页修改再生成增强
3. DOCX 与 PPT 页面对齐优化
4. 页级证据展示增强

## P2（可后置）

1. 语音输入
2. 视频解析
3. HTML5 小互动页
4. 更复杂的课堂智能体
5. 更复杂的多模态互动能力

---

## 14. 预期收益

完成本次改造后，预期可以获得以下收益：

### 1. PPT 成品质量显著提升

- 页面内容更丰富
- 布局更稳定
- 视觉完成度更高
- 页间节奏更完整

### 2. 保留当前项目比赛优势

- 本地知识库 RAG
- 三源融合
- 教学证据链
- 工作台与再生成
- 导出能力

### 3. 降低无效重构成本

- 不推翻现有教学系统
- 不重写工作台 UI
- 不重写导出层
- 只替换最关键的生成内核

### 4. 答辩叙事更完整

可形成一条清晰主线：

> 教师需求输入 → 多轮澄清 → 资料与知识库融合 → 高质量课件生成 → 工作台优化 → 标准格式导出

这比单纯强调“做了一个文生 PPT”更有竞争力。

---

## 15. 最终建议

综合赛题要求、当前项目进度与实际质量问题，建议采用以下最终路线：

# **保留“教师需求 + 参考资料 + FastGPT 知识库”三源融合框架，替换底层 PPT 生成内核为 OpenMAIC 原生 scene workflow 的精简版，并以 TeachingDesign 作为展示与编辑视图进行承接。**

这是当前最合理的改造方向，原因如下：

1. 最符合赛题要求的系统叙事
2. 最大化复用现有实现成果
3. 最能直接提升当前最关键的 PPT 质量问题
4. 实施风险和开发成本相对可控
5. 适合比赛时间线下快速形成高质量展示版本

---

## 16. 下一步落地建议

建议下一步按以下顺序推进：

1. 先做 `TeachingGenerationSpec` 与 `scene-based generation` 的旁路 PoC
2. 用相同输入、相同模型，对比当前教学链路与新 scene 链路的 PPT 输出质量
3. 若质量显著提升，则将 scene-based pipeline 升级为主流程
4. 保留 `TeachingDesign` 工作台作为统一展示与导出层
5. 在比赛版中优先交付稳定 PPT、DOCX、证据链与 quiz 能力

---

如果后续需要继续细化，可以在本方案基础上进一步拆出：

- 文件级改造清单
- 数据结构改造方案
- Prompt 改造方案
- 第一阶段开发任务拆解表
- 比赛答辩版 PPT 讲稿结构
