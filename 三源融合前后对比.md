# 三源融合前后对比分析

## 原始实现（三源融合之前）

### 文件位置
`OpenMAIC/lib/generation/teaching-outline-generator.ts` (commit 28defcb)

### 上下文构造方式

#### 1. RAG 知识库查询
```typescript
// Step 1: Query FastGPT knowledge base if enabled
let ragContext = '';

if (request.useKnowledgeBase) {
  try {
    const query = buildKnowledgeQueryFromTeachingRequest(request);
    const result = await queryFastGPT(query, { timeoutMs: 300000 });
    ragContext = result.answer;
  } catch (error) {
    log.warn('FastGPT query failed, continuing without knowledge base enhancement:', error);
    ragContext = '';
  }
}
```

#### 2. 图片描述构建
```typescript
// Build available images description
let availableImagesText = request.language === 'zh-CN' ? '无可用图片' : 'No images available';

if (pdfImages && pdfImages.length > 0) {
  if (options?.visionEnabled && options?.imageMapping) {
    // Vision mode: 部分图片作为附件，部分作为文本描述
    const allWithSrc = pdfImages.filter((img) => options.imageMapping![img.id]);
    const visionSlice = allWithSrc.slice(0, MAX_VISION_IMAGES);
    const textOnlySlice = allWithSrc.slice(MAX_VISION_IMAGES);
    const noSrcImages = pdfImages.filter((img) => !options.imageMapping![img.id]);

    const visionDescriptions = visionSlice.map((img) =>
      formatImagePlaceholder(img, request.language),
    );
    const textDescriptions = [...textOnlySlice, ...noSrcImages].map((img) =>
      formatImageDescription(img, request.language),
    );
    availableImagesText = [...visionDescriptions, ...textDescriptions].join('\n');
  } else {
    // Text-only mode: 所有图片作为文本描述
    availableImagesText = pdfImages
      .map((img) => formatImageDescription(img, request.language))
      .join('\n');
  }
}
```

#### 3. Prompt 构造（直接内联）
```typescript
// Prepare RAG context section (truncate to avoid token overflow)
const safeRagContext = ragContext ? ragContext.slice(0, 2000) : '';
const ragSection = safeRagContext
  ? `\n## 【知识库参考内容】\n${safeRagContext}\n`
  : '';

const userPrompt = `# 教学设计任务

## 基本信息
- 学科：${request.subject}
- 课题：${request.topic}
- 年级：${request.gradeLevel}
- 课时：${request.duration} 分钟

## 教学目标
${request.objectives ? formatObjectives(request.objectives) : '（请根据课题自动生成三维目标）'}
${ragSection}
## 参考资料内容
${pdfText ? pdfText.substring(0, MAX_PDF_CONTENT_CHARS) : '无'}

## 可用图片资源
${availableImagesText}

## 特殊要求
${request.additionalNotes || '无'}

---

请基于以上信息，生成完整的教学设计JSON。`;
```

### 特点分析

#### ✅ 优点
1. **简单直接**：所有逻辑在一个函数中，易于理解
2. **已经支持 RAG**：FastGPT 知识库查询已集成
3. **已经支持 PDF**：可以处理 PDF 文本和图片

#### ❌ 局限性
1. **缺少参考资料解析层**：
   - 只能处理已解析的 `pdfText` 和 `pdfImages`
   - 不支持多个资料文件
   - 不支持图片直接上传

2. **上下文构造分散**：
   - RAG 处理在一处
   - PDF 处理在另一处
   - Prompt 构造又在另一处
   - 难以维护和扩展

3. **缺少结构化的三源融合**：
   - 没有明确的"三源"概念
   - 各部分内容简单拼接
   - 没有统一的上下文包结构

4. **长度控制不统一**：
   - RAG: 2000 字符
   - PDF: MAX_PDF_CONTENT_CHARS
   - 没有整体的长度管理策略

---

## 新实现（三源融合之后）

### 新增文件

#### 1. `lib/generation/teaching-material-parser.ts`
**职责**：统一的资料解析层

```typescript
export interface ParsedMaterialsResult {
  textContent: string;      // 所有资料的文本内容拼接
  images: ParsedImage[];    // 所有可用图片
  summaries: string[];      // 每个资料的简短概述
}

export async function parseTeachingMaterials(
  materials: ReferenceMaterial[]
): Promise<ParsedMaterialsResult>
```

**功能**：
- 支持多种资料类型（PDF、图片、文本）
- 统一的解析接口
- 优雅的错误处理

#### 2. `lib/generation/teaching-context-builder.ts`
**职责**：三源融合上下文构建

```typescript
export function buildTeachingContextBundle(
  request: TeachingRequest,
  parsedMaterials: ParsedMaterialsResult,
  ragContext: string
): TeachingContextBundle
```

**功能**：
- 结构化的三源整合
- 统一的长度控制
- 格式化的 mergedContext 输出

### 改造后的流程

#### 1. 资料解析（新增）
```typescript
// Step 1: Parse reference materials
let parsedMaterials = {
  textContent: '',
  images: [] as ParsedImage[],
  summaries: [] as string[],
};

if (materials && materials.length > 0) {
  try {
    parsedMaterials = await parseTeachingMaterials(materials);
    log.info(`Materials parsed: ${parsedMaterials.textContent.length} chars, ${parsedMaterials.images.length} images`);
  } catch (error) {
    log.error('Failed to parse materials, continuing without them:', error);
  }
}
```

#### 2. RAG 查询（保持不变）
```typescript
// Step 2: Query FastGPT knowledge base if enabled
let ragContext = '';

if (request.useKnowledgeBase) {
  try {
    const query = buildKnowledgeQueryFromTeachingRequest(request);
    const result = await queryFastGPT(query, { timeoutMs: 300000 });
    ragContext = result.answer;
  } catch (error) {
    log.warn('FastGPT query failed, continuing without knowledge base enhancement:', error);
    ragContext = '';
  }
}
```

#### 3. 三源融合（新增）
```typescript
// Step 3: Build three-source context bundle
const contextBundle = buildTeachingContextBundle(request, parsedMaterials, ragContext);

log.info('Three-source context bundle built:', {
  materialChars: contextBundle.extractedFromMaterials.textContent.length,
  imageCount: contextBundle.extractedFromMaterials.availableImages.length,
  ragChars: contextBundle.retrievedKnowledge.relevantChunks.join('').length,
  mergedChars: contextBundle.mergedContext.length,
});
```

#### 4. Prompt 构造（简化）
```typescript
// Step 5: Build prompt using merged context
const systemPrompt = `你是一位经验丰富的教学设计专家。
你的任务是根据教师需求、参考资料和知识库内容，生成结构化的教学设计。
...
5. 充分融合参考资料和知识库的内容，确保教学设计的专业性和完整性`;

// Use merged context from three-source bundle
const userPrompt = `${contextBundle.mergedContext}

## 可用图片资源
${availableImagesText}

---

请基于以上三源融合信息，生成完整的教学设计JSON。`;
```

---

## 对比总结

### 原始实现的 Prompt 结构
```
# 教学设计任务

## 基本信息
- 学科、课题、年级、课时

## 教学目标
（教师指定或自动生成）

## 【知识库参考内容】
（FastGPT 查询结果，最多 2000 字符）

## 参考资料内容
（PDF 文本，最多 MAX_PDF_CONTENT_CHARS）

## 可用图片资源
（图片列表）

## 特殊要求
（教师的额外说明）
```

**问题**：
- 结构扁平，各部分地位不清晰
- 没有明确的"三源"概念
- 简单拼接，缺少整合说明

### 新实现的 Prompt 结构
```
# 【教师需求】
学科、课题、年级、课时
教学目标（知识、能力、情感态度）
特殊要求

# 【参考资料内容】
## 【资料1.pdf】
...内容...

## 【资料2.pdf】
...内容...

（内容较长，已截取前3000字）

# 【参考资料中的图片/图示资源】
共有 X 张可用图片，可在课件中引用
（图片ID将在后续生成中提供）

# 【知识库参考内容】
...FastGPT 查询结果...
（内容较长，已截取前2000字）

# 【生成要求】
请综合以上三方面信息：
1. 教师的明确需求和教学目标
2. 参考资料中的具体内容和图片资源
3. 知识库中的专业知识和教学建议

生成结构化的教学设计，确保：
- 教学内容准确、完整
- 充分利用参考资料中的素材
- 融入知识库的专业指导
- 符合教师的特殊要求
```

**优势**：
- ✅ 结构清晰，三源分明
- ✅ 明确的融合指导
- ✅ 每个来源都有清晰的标识
- ✅ 统一的长度控制策略
- ✅ 更好的可维护性和可扩展性

---

## 架构改进对比

### 原始架构
```
teaching-outline-generator.ts
  ├─ 内联 RAG 查询
  ├─ 内联图片处理
  ├─ 内联 Prompt 构造
  └─ 生成教学设计
```

**特点**：
- 单文件实现
- 逻辑耦合
- 难以扩展

### 新架构
```
teaching-outline-generator.ts
  ├─ parseTeachingMaterials()          [teaching-material-parser.ts]
  ├─ queryFastGPT()                    [fastgpt-client.ts]
  ├─ buildTeachingContextBundle()      [teaching-context-builder.ts]
  └─ 生成教学设计
```

**特点**：
- 职责分离
- 模块化设计
- 易于测试和扩展

---

## 功能对比表

| 功能 | 原始实现 | 新实现 |
|------|---------|--------|
| **教师需求** | ✅ 支持 | ✅ 支持 |
| **FastGPT RAG** | ✅ 支持 | ✅ 支持 |
| **PDF 解析** | ✅ 支持（单个） | ✅ 支持（多个） |
| **图片上传** | ❌ 不支持 | ✅ 支持 |
| **多资料融合** | ❌ 不支持 | ✅ 支持 |
| **结构化上下文** | ❌ 简单拼接 | ✅ 三源融合 |
| **长度控制** | ⚠️ 分散 | ✅ 统一管理 |
| **错误降级** | ✅ 支持 | ✅ 增强 |
| **可扩展性** | ⚠️ 一般 | ✅ 优秀 |
| **代码可维护性** | ⚠️ 一般 | ✅ 优秀 |

---

## 核心改进点

### 1. 从"两源"到"三源"
**原始**：教师需求 + RAG（PDF 只是作为参考资料文本）
**新版**：教师需求 + 参考资料（结构化） + RAG

### 2. 从"单资料"到"多资料"
**原始**：只能处理一个 PDF 的文本和图片
**新版**：支持多个 PDF、多张图片、多种资料类型

### 3. 从"简单拼接"到"结构化融合"
**原始**：各部分内容简单拼接到 prompt
**新版**：明确的三源结构 + 融合指导

### 4. 从"分散处理"到"模块化架构"
**原始**：所有逻辑在一个函数中
**新版**：解析层 → 上下文层 → 生成层

### 5. 从"隐式"到"显式"
**原始**：三源概念隐藏在代码中
**新版**：TeachingContextBundle 显式表达三源融合

---

## 总结

原始实现已经具备了 RAG 和 PDF 处理的基础能力，但：
1. **缺少统一的资料解析层**
2. **缺少结构化的三源融合概念**
3. **上下文构造分散且难以维护**

新实现通过引入：
1. **`teaching-material-parser.ts`** - 统一资料解析
2. **`teaching-context-builder.ts`** - 三源融合构建
3. **改造 `teaching-outline-generator.ts`** - 使用新的模块

实现了：
- ✅ 真正的"三源融合"概念
- ✅ 支持多资料、多类型
- ✅ 结构化、可扩展的架构
- ✅ 更清晰的 prompt 结构
- ✅ 更好的代码可维护性

这是一次从"能用"到"好用"的架构升级！
